"""Curated curriculum source: each unit is authored independently, not expanded from templates."""
import json
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1]
imports='''import Term from '../../components/Term.astro';
import Slide from '../../components/Slide.astro';
import Diagram from '../../components/Diagram.astro';
import Demo from '../../components/Demo';
import {asset} from '../../data/curriculum';
'''
def write(id,title,description,body,terms,sources,question,answer,sessions=[],lab=None,kind='lessons',prerequisites=None):
 order=int(id[:2]) if id[:2].isdigit() else 1
 data=dict(title=title,description=description,order=order,module=min(7,(order-1)//3),minutes=25,prerequisites=prerequisites if prerequisites is not None else ([previous] if kind=='lessons' and (previous:=globals().get('last')) else []),terms=terms.split(),sources=sources.split(),sessions=sessions,question=question,answer=answer)
 if lab:data['lab']=lab
 (ROOT/f'src/content/{kind}/{id}.mdx').write_text('---\n'+'\n'.join(k+': '+json.dumps(v,ensure_ascii=False) for k,v in data.items())+'\n---\n'+imports+'\n'+body.strip()+'\n')
 if kind=='lessons':globals()['last']=id

write('01-request','一个请求，怎样到达 GPU？','用一个问答请求，区分应用、推理引擎和基础设施的职责。',r'''
你在聊天框输入“解释一下 KV Cache”，屏幕开始逐字输出。眼前只有一个应用，背后却存在两个不同的过程：**一次请求正在被执行，一套系统正在被持续维护**。先分清它们，后面遇到 Kubernetes、vLLM、NCCL 等名字时，才知道各自在解决什么问题。

## 先沿着请求走一遍

客户端把文字和生成参数交给服务接口。网关可能执行身份验证、限流和路由，然后把请求交给推理引擎。这里的引擎不是模型权重本身，而是负责组织模型执行的软件。

引擎将输入转成 <Term id="token" /> ID，决定请求何时进入计算、可以和哪些请求一起处理、要为上下文分配多少显存。真正执行矩阵运算的是 <Term id="gpu" />；结果回到引擎，经采样得到输出 token，再通过网络发给客户端。

<Diagram id="request" caption="请求数据流与资源控制流：上方处理请求，下方维持可运行的系统。" />

模型文件、推理引擎、服务接口三者不能互换。模型文件保存参数；引擎加载并执行它；接口约定客户端如何表达请求。两个服务都提供相似接口，不代表它们使用相同模型、调度方式或性能配置。

## 再看谁让它一直可用

假设推理进程退出，正在进行的请求可能失败。Kubernetes 的 <Term id="controller" /> 可以发现副本不足，创建新的 <Term id="pod" />。但新 Pod 还要下载镜像、访问模型、获得设备、初始化通信并完成加载。恢复一个进程并不等于瞬间恢复用户体验。

这条维护路径通常叫**控制面**：描述期望状态、决定资源分配、协调实际状态。请求实际经过的路径叫**数据面**。它们会影响彼此：控制面调度不合理会让数据面变慢；数据面的观测结果也可以用于控制面的扩容决策。

| 问题 | 首先观察哪里 | 为什么 |
|---|---|---|
| 请求连接失败 | 网关、Service、服务进程 | 请求可能还没到模型 |
| 很久才出第一个 token | 队列、Prefill、加载与网络 | 时间可能花在等待而非计算 |
| 多卡服务一直启动不了 | rank 发现、设备、网络 | 一个参与者失败可能阻断整个组 |
| Pod 不断重建 | 控制器、探针、退出原因 | 重试可能掩盖真正失败 |

## 用一次请求建立自己的地图

选择一个简单的文本生成服务，在纸上标出客户端、网关、引擎、GPU 四个位置。然后在旁边补上三个资源：模型文件、上下文缓存、容器配置。每条连线写清楚传的是请求、数据还是控制指令。

别急着添加几十个项目名称。一个组件如果还不能对应到具体问题，就先把问题讲清楚。例如“需要让多个 GPU 交换中间结果”比先记住 NCCL 更有帮助。

## 从会议回到这个问题

可观测性场次从业务层一直画到 GPU。它提醒我们：某一层的指标很健康，不能直接证明整个请求很健康。后面会用同一张地图追踪三种故障，并把每一层的证据连接起来。

<Slide id="llm-observability-7" minute="本场强调从 token 的用户体验出发，沿引擎、Kubernetes、主机和 GPU 逐层下钻。分层图提供定位地图，但排障还需要 trace 与指标关联。" />

## 一个可以立即做的练习

把“GPU 利用率 95%，所以服务没问题”拆成两个陈述：设备在采样窗口中很忙；用户请求达到了目标。前者能直接证明后者吗？如果不能，还缺哪些观测？写下首 token 时间、输出吞吐、错误率，以及请求是否正在排队。你已经开始用系统语言思考了。
''','token gpu pod controller','k8s vllm-metrics','如果 Kubernetes 把退出的推理 Pod 重建了，为什么用户请求仍可能持续失败？','Pod 创建只是恢复过程的一步。设备准备、模型加载、通信初始化、就绪检查、路由恢复都需要时间；在途请求也不一定可以透明重试。',['llm-observability'])

write('02-kubernetes','谁在运行服务，谁在管理服务？','从进程和容器出发，理解 Pod、Service、控制器与调度器。',r'''
先想一个普通 Python 服务：操作系统启动进程，进程监听端口，退出后就停止提供服务。把它放进容器，主要改变的是运行环境和资源边界；并没有自动解决多机调度、故障恢复和稳定访问。

## 进程、容器与 Pod 是三层概念

**进程**是正在运行的程序。**容器**利用命名空间隔离视图、通过 cgroups 约束资源，并用镜像提供文件系统。它通常共享宿主机内核，不是完整虚拟机。<Term id="pod" /> 是 Kubernetes 调度与生命周期管理的基本单元，可以包含多个协作容器。

镜像像可分发的运行环境，容器是它的一次运行。Pod 中的容器共享网络命名空间，所以可以通过 localhost 通信；不同 Pod 通常拥有不同 IP。不要把 Pod IP 当成永久地址，因为 Pod 会被替换。

## 声明式系统如何工作

你提交一个 Deployment，声明“需要两个服务副本”。API Server 接收并保存对象。控制器观察现状，创建需要的 Pod。调度器为未调度 Pod 找节点，节点上的 kubelet 再协调容器运行时启动容器。

<Diagram id="reconcile" caption="期望状态与实际状态不断对照；调度决定落点，kubelet 负责节点执行。" />

<Term id="controller" /> 不会保证所有故障都能自动修好。如果镜像地址写错，每次创建都会继续失败。它擅长保持声明，而声明本身仍需要正确。

## Running、Ready 和健康不能混为一谈

Running 是 Pod 生命周期阶段，通常表示已绑定节点且容器已创建，至少有容器运行、启动或重启。Ready 是能否接流量的条件。分布式模型还有更高层的不变量：所有 rank 是否到齐？通信是否通过？每个节点的模型版本是否一致？

探针也有不同职责：startupProbe 给慢启动留时间，readinessProbe 控制就绪，livenessProbe 处理进程失活。把昂贵且容易抖动的模型检查放进存活探针，可能触发无意义重启。

```yaml
# 配置片段：需放在一个 container 下，不是完整 Deployment。
ports:
  - containerPort: 8000
readinessProbe:
  httpGet:
    path: /health
    port: 8000
  periodSeconds: 10
startupProbe:
  httpGet:
    path: /health
    port: 8000
  periodSeconds: 10
  failureThreshold: 60
```

这里 startupProbe 最多提供约 600 秒的探测窗口，具体时序还受初始延迟和请求超时影响。这是教学参数，不应照搬到所有模型。`/health` 的含义也由应用实现决定。

## Service 为什么有用

Service 为一组 Pod 提供稳定访问抽象，通常通过标签选择后端。它并不自动理解哪个模型副本缓存了某个前缀，也不会替你实现所有 LLM 调度策略。基础网络负载均衡与缓存感知路由是不同问题。

观察时按层分开：`kubectl get pods` 看阶段，`kubectl describe pod` 看事件，容器日志看进程失败，Service 和 EndpointSlice 看后端选择。遇到故障，一次只验证一个假设。

## 将它映射到推理系统

一个模型副本可能需要多个 Pod。单个 Deployment 的副本数不天然表达 leader/worker 关系；这正是后面 LWS 的用途。设备也不只是“请求 8 个 CPU”，还会涉及 GPU 类型、健康和拓扑；后面用 DRA 继续补全地图。
''','pod controller lws','k8s lws-api','Deployment、Service、调度器分别负责什么？','Deployment 维持声明的副本与更新行为；Service 提供稳定访问与后端选择；调度器为尚未调度的 Pod 选择节点。它们都不直接计算模型 token。',lab='01-serving')

write('03-attention','模型怎样利用前面的文字？','用一个两位置例子看懂 Q、K、V，然后理解为什么可以缓存。',r'''
解释 KV Cache 前，先回答一个更小的问题：模型正在处理当前位置，它如何从前面的内容中取出相关信息？<Term id="attention" /> 提供了一种可学习的加权汇总方式。

## 把形状说清楚

<Term id="tensor" /> 是带形状的多维数组。设一个简化输入有 3 个 token，每个表示有 4 个数字，可以把它写成形状 `[3,4]` 的矩阵 X。这里的数字不是汉字编码，而是网络中的连续向量表示。

线性变换产生查询 Q、键 K 和值 V：`Q = XWq`、`K = XWk`、`V = XWv`。可以把 Q 理解为“当前位置拿什么特征找信息”，K 是“每个位置提供什么匹配特征”，V 是“匹配后汇总的内容表示”。这只是直觉，三者都是学习得到的数值，不是显式关键词。

## 做一次最小注意力计算

设当前查询 `q=[1,0]`，两个历史键为 `[1,0]` 和 `[0,1]`，值分别为 `[10,0]` 和 `[0,10]`。点积得到分数 1 和 0。再除以键维度平方根 $\sqrt{2}$，并通过 softmax 转成和为 1 的权重，大约为 0.67 和 0.33。因此汇总结果约为 `[6.7,3.3]`。

$$\operatorname{Attention}(Q,K,V)=\operatorname{softmax}\left(\frac{QK^\top}{\sqrt{d_k}}+M\right)V$$

其中 $d_k$ 是键向量维度；$M$ 是掩码，不允许关注的位置加入负无穷，使其 softmax 权重为零。因果语言模型在普通自回归生成中不能读取未来位置。

```python
import math
q = [1.0, 0.0]
keys = [[1.0, 0.0], [0.0, 1.0]]
values = [[10.0, 0.0], [0.0, 10.0]]
scores = [sum(a*b for a, b in zip(q, k)) / math.sqrt(2)
          for k in keys]
exp_scores = [math.exp(s - max(scores)) for s in scores]
weights = [s / sum(exp_scores) for s in exp_scores]
out = [sum(w*v[j] for w, v in zip(weights, values)) for j in range(2)]
print(weights, out)
```

减去最大分数不改变 softmax 比例，却有助于避免指数溢出。这里的代码只展示数学含义，不代表 GPU 的高性能实现方式。

## 为什么历史 K/V 可以留下来

在同一模型、同一上下文前缀和相同必要计算条件下，历史位置的 K/V 不需要因为生成了新 token 而全部重算。新的位置生成自己的 Q/K/V，把新 K/V 追加进去，再用新的 Q 读取历史 K/V。

这就是 <Term id="kv-cache" /> 的基本出发点。缓存省掉的是历史中间表示的重复计算；新位置仍需要读取相应历史信息。上下文变长时，缓存本身和注意力读取成本都会增长。

## 多头与分组查询改变什么

一个注意力头是一套查询与汇总通道。MHA 为各查询头提供对应 K/V 头；MQA 让多个查询头共享一组 K/V；GQA 则分组共享。因此计算 KV 大小时要用 **KV head 数**，不能看到模型有 32 个查询头，就机械乘 32。

这里讲的是典型 Transformer 注意力。滑动窗口、混合状态空间模型、特殊 KV 压缩等结构会改变缓存布局和保留范围，后续容量公式都明确限定适用范围。

## 把知识转成检查动作

找一个模型的配置文件，定位层数、查询头数、KV head 数和 hidden size。若 head dimension 没有单独给出，不能在所有架构中都无条件用 hidden size 除查询头数；先看具体模型实现。把查到的字段和来源一起记下来，再进入显存估算。
''','tensor attention token kv-cache','attention pagedattention','为什么生成新 token 时，不需要重算完整历史 K/V？这是否意味着长上下文不再增加计算？','因果前缀在相同模型与计算条件下对应的历史 K/V 可复用。但新查询仍需读取并处理可见历史，所以历史长度依然影响缓存容量和注意力工作。',lab='02-attention')

write('04-hardware','GPU 的时间花在计算，还是搬数据？','区分容量、带宽、延迟和计算吞吐，用一个下界模型定位瓶颈。',r'''
“这张 GPU 很快”并不能回答某个模型是否会快。一个工作负载既要搬数据，又要执行运算；任何一个环节跟不上，都可能使另一部分等待。

## 三种容易混淆的硬件量

**容量**告诉你能放多少数据，例如 80 GiB。<Term id="bandwidth" /> 告诉你每秒能搬多少，例如有效 GB/s。**延迟**告诉你一次操作多久开始得到结果。装得下、搬得快、响应快，是三个问题。

模型权重通常先从存储进入主内存，再通过设备互连进入 GPU 内存。执行时，数据在设备内存、缓存、共享内存和寄存器之间流动。具体路径受硬件和 kernel 实现影响，不是每个数据都必须显式经过每一级。

<figure className="knowledge-image"><img src={asset('images/gpu-data-movement.webp')} alt="CPU 主内存通过 PCIe 连接 GPU 显存，计算单元通过片上存储复用数据" loading="lazy"/><figcaption>原创知识图：重点区分设备间搬运与 GPU 内部的数据复用。不同 GPU 的显存类型和容量不同。</figcaption></figure>

## 给执行时间估一个下界

设算子需要 $F$ 次浮点运算，至少搬运 $B$ 字节数据。硬件有效计算速度为 $P$ FLOP/s，有效带宽为 $W$ byte/s。那么理想时间下界可以写成：

$$T\ge\max(F/P,\ B/W)$$

例如一次工作需要 20 GFLOP、读取和写入共 4 GB。假设有效计算速度 100 TFLOP/s、有效带宽 1 TB/s，计算项约 0.2 ms，传输项约 4 ms。即使把计算峰值翻倍，主要的 4 ms 搬运约束仍在。

这些数字是说明方法的教学假设，不能用作 GPU benchmark。真实时间还包括 kernel launch、同步、布局转换及未重叠部分。

## Roofline 在解释什么

<Term id="roofline" /> 用操作强度 $I=F/B$，把性能约束写为 `min(P, W×I)`。当每搬一个字节只做很少运算，带宽更容易成为瓶颈；增加数据复用或融合操作，可以减少 B，从而提高操作强度。

融合也会有代价。一个 kernel 同时做更多工作，可能消耗更多寄存器和共享内存，减少可并发执行的线程块。优化不是“融合越多越好”，而是基于实际 shape、硬件和测量做取舍。

## 为什么 Decode 经常需要关注带宽

小 batch 的普通 Decode 每个序列只推进很少 token，却要访问许多权重与历史 KV，因此经常具有较低操作强度。增大 batch 可以让权重读取服务更多 token，但也增加 KV 占用和排队。Prefill 通常处理更多输入位置，矩阵计算规模更大，但也不是所有配置下都必然计算受限。

## 用讲义理解减少搬运的价值

<Slide id="speedy-optimizers-4" minute="优化器短场次把横向融合与纵向融合区分开：前者批量处理多个 tensor，后者把连续更新步骤放进更少的 kernel，从而减少中间写回和 launch。" />

阅读这张图时先找中间结果：如果每一步都写回显存，下一步再读入，会重复花费带宽。再找融合后仍必须保留的状态：不能为了少读写而破坏算法正确性。
''','gpu hbm bandwidth roofline kernel','cuda optim','一个算子主要受内存带宽限制，换成计算峰值翻倍但带宽不变的 GPU，一定会快很多吗？','不一定。先估计并测量实际字节流量、带宽和操作强度。可能需要减少读写或增加复用；更高计算峰值只有在计算约束重要时才产生明显收益。',['speedy-optimizers','kernelagent'],lab='03-kernel')

write('05-pytorch','一行 PyTorch 怎样变成 GPU 工作？','理解 eager、编译、kernel launch 和异步执行，再谈性能优化。',r'''
写下 `y = x * 2 + 1`，看起来只有一个表达式，但底层可能发生多次 GPU 工作提交。高级代码短，不等于执行路径短；同样，代码行数多也不一定慢。

## 从算子到 kernel

PyTorch 中的算子表达张量操作的语义，例如乘法、加法和矩阵乘法。底层实现可能调用硬件库或生成 <Term id="kernel" />。一个算子可以由多个 kernel 实现，多个算子也可能被融合进一个 kernel。

Eager 模式通常随着 Python 执行逐步调度操作。`torch.compile` 尝试捕获可编译部分，通过后端进行优化。编译不意味着所有 Python 逻辑都能无条件变成一个 GPU kernel：动态行为、图中断、形状变化都会影响实际路径。

## 一个可观察的表达式

```python
import torch

def pointwise(x):
    return torch.relu(x * 2 + 1)

x = torch.randn(1024, 1024, device='cuda')
compiled = torch.compile(pointwise)
expected = pointwise(x)
actual = compiled(x)
torch.testing.assert_close(actual, expected)
```

这段代码先检查正确性。首次调用 `compiled` 可能包括编译，因此不能拿它与已经热身的 eager 单次运行直接比较。不同 shape 或输入类型还可能触发重新编译。

## GPU 执行通常是异步的

CPU 提交工作后可能立即继续。若用 Python 时钟只围住一个调用，测到的可能主要是提交时间，而不是 GPU 完成时间。使用 CUDA events 或在计时边界同步，才能把完成等待包含进去。

```python
# 独立计时示意；性能实验还需多次重复与统计。
for _ in range(10):
    compiled(x)
torch.cuda.synchronize()
start = torch.cuda.Event(enable_timing=True)
end = torch.cuda.Event(enable_timing=True)
start.record()
for _ in range(100):
    compiled(x)
end.record()
end.synchronize()
print('平均 GPU 时间(ms):', start.elapsed_time(end) / 100)
```

这仍不包含完整服务端排队、Python 业务处理和网络时间。计时口径必须跟你的问题一致。

## 优化前先固定比较条件

固定 dtype、shape、设备、输入分布和容差。数值容差需要根据算法与精度选择，而不是为了让错误通过随意放宽。记录热身、重复次数、异常值处理及中位数或分位数。

<Slide id="kernelagent-11" minute="KernelAgent 强调 correctness-first：没有通过正确性验证的候选不能报告性能收益。优化应输出完整可运行产物、测试和调优日志。" />

## 与框架通用化的联系

GPU 不是唯一加速器。PyTorch 的统一入口尝试把设备管理等公共行为抽象出来，但某些 kernel 和 profiler 仍然属于具体硬件。统一 API 让上层代码更易维护，不代表不同后端支持完全相同，也不代表性能可直接迁移。进阶专题会进一步拆解这条边界。
''','tensor kernel gpu roofline','pytorch compile accelerator','为什么首次 torch.compile 调用和没有同步的 Python 计时，容易导致错误的性能结论？','首次调用可能包含编译；没有同步的 CPU 计时可能只覆盖提交。需要先热身、明确 GPU 完成边界，并记录端到端和设备侧的不同计时口径。',['kernelagent','pytorch-generalization'],lab='03-kernel')

write('06-vllm','部署第一个可以测量的推理服务','从固定镜像、模型与参数开始，让服务可访问，也让实验可复查。',r'''
第一个部署的目标不是“越大越好”，而是建立一个可重复的起点：知道运行的模型、软件版本、资源条件和请求参数，并能保存响应与观测结果。

## 先分清四个版本

服务镜像决定 vLLM 与其依赖；模型 revision 决定权重和配置；GPU 驱动影响设备兼容性；启动参数决定并行方式和资源预算。只记录“用了 vLLM”不足以复现实验。

本教材将 vLLM 服务实验固定到 `v0.29.0`。模型使用一个小型公开模型作为起点，模型仓库 revision 需要在实验准备阶段解析成具体 commit，并记录到环境文件中。标签与模型名称都可能变化，内容 hash 更适合长期复查。

## 启动容器时每个参数都应该有目的

```bash
docker run --rm --name inference-lab --gpus all \
  -p 127.0.0.1:8000:8000 --shm-size 2g \
  -v "$PWD/model-cache:/root/.cache/huggingface" \
  vllm/vllm-openai:v0.29.0 \
  --model Qwen/Qwen2.5-0.5B-Instruct \
  --served-model-name lab-model \
  --max-model-len 4096 \
  --gpu-memory-utilization 0.8
```

`--gpus all` 让容器可访问设备，但模型并行度仍由引擎配置决定。端口只绑定本机是为了这个单机实验。模型缓存卷避免每次重建重新下载。`--max-model-len` 限制可接受序列长度；`--gpu-memory-utilization` 是引擎内存预算参数，并不是 GPU 实际忙碌比例。

这段命令用于解释结构。执行正式实验时使用实验包的 `serve.sh`，它还要求固定 `MODEL_REVISION` 并保存镜像信息。

## 从最小请求验证开始

```bash
curl http://127.0.0.1:8000/v1/models
curl http://127.0.0.1:8000/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"lab-model","messages":[{"role":"user","content":"用一句话解释缓存"}],"max_tokens":64,"temperature":0,"stream":true}'
```

第一条确认服务可访问并查看对外模型名称。第二条请求流式输出。`temperature=0` 减少采样随机性，但不保证不同硬件、后端或执行批次之间完全逐位一致。

## 成功响应之后还要检查什么

读取 `/metrics`，观察是否存在请求与引擎相关指标。保存启动日志中的软件版本、可用 KV 预算和模型配置。指标名称可能随版本变化，先读当前端点的 HELP 与 TYPE，再写查询。

不要把模型下载、冷启动、编译热身和稳定服务混成一个耗时。冷启动本身值得测，但要单独成组记录。

## 常见失败的定位顺序

| 现象 | 优先检查 | 不应直接得出的结论 |
|---|---|---|
| 容器看不到 GPU | runtime、驱动、设备暴露 | vLLM 模型不支持 |
| 启动时 OOM | 权重、长度预算、并行度、其他进程 | 一定是 KV Cache 泄漏 |
| HTTP 404/模型找不到 | 接口路径、served model name | GPU 不健康 |
| 长时间没有响应 | 加载日志、队列、引擎状态 | 一定是网络慢 |

接下来我们会让这个服务承担多个负载，而每次只改一个变量。这样才能把“快了”变成有证据的解释。
''','gpu kv-cache throughput','vllm vllm-serving vllm-metrics','gpu-memory-utilization=0.8 是否意味着 GPU 的计算利用率会保持 80%？','不是。它影响引擎的 GPU 内存预算，计算忙碌程度由负载、算子、调度和数据搬运共同决定。',lab='01-serving')

write('07-latency','为什么首个 token 很慢？','先把排队、Prefill 和输出拆开，再判断应该优化哪里。',r'''
假设你发出一个问题，等了 15 秒才看到第一段回答，但之后输出很快。另一台服务 1 秒就开始输出，却每隔半秒才出现一点内容。两者都可能被简单的“总耗时”掩盖；用户感受到的慢，其实发生在不同阶段。

## 首 token 之前，时间流向哪里

<Term id="ttft" /> 是从约定起点到首个输出 token 的时间。客户端口径可能包括网络、网关、排队、分词、调度和 Prefill；引擎内部口径可能只包含其中一部分。比较两个数字前，先确认起止事件一致。

<Term id="prefill" /> 处理输入前缀并构建缓存，通常能在一次前向计算中并行处理多个位置。首次采样产生输出后，后续生成进入 <Term id="decode" />。前者和后者对资源的需求不同，因此“首 token 快”和“持续输出快”需要分别测量。

## 一张图，逐段看清等待

先看原图上方的完整请求，再看中间的等待和计算。图上各条横线不一定首尾无缝相连；未画出的空隙可能包含其他调度或处理阶段，不能直接把两项相加后认定解释了全部延迟。

<Slide id="llm-observability-11" steps={['先找到 Queue wait：8.1 秒发生在 Prefill 之前，不能通过只加速模型运算消除。','再看 Prefill：6.0 秒是输入计算阶段，首 token 在约 15.7 秒出现。','最后看 Decode 与 Stream：后续输出是另一段时间，流畅程度需要另外测量。']} minute="讲义把一个流式请求按阶段展开，配合本场“从现象定位阶段，再检查相邻层指标”的方法。这里只用图中时间解释诊断过程，不把它当成硬件基准。" />

## 用一个可以算清的模型练习

设排队时间为 $Q$，Prefill 为 $P$，网络与其他固定开销为 $O$，输出 token 数为 $N$，后续每 token 平均时间为 $D$：

$$TTFT=Q+P+O,\qquad T_{total}=TTFT+(N-1)D$$

为什么是 $N-1$？因为第一个输出已经在 TTFT 的终点产生。这里假设后续步骤耗时固定，真实的连续批处理、网络 chunk 与推测解码会让情况更复杂。

<Demo type="latency" client:visible />

先将队列调到很大，保持其他参数不变；再把 Prefill 缩短一半。如果 TTFT 改善很有限，你看到的是“优化了非主要部分”的结果。接着把输出长度调长，观察总时间如何增长，而 TTFT 保持不变。这让你区分两个不同目标。

## 平均速度不能说明全部体验

<Term id="tpot" /> 描述平均输出时间；<Term id="itl" /> 关注相邻 token 间隔。平均 40 ms 的输出，可能每一步都接近 40 ms，也可能大部分很快、中间停顿很久。用户对后一种更敏感。

需要查看分布和分位数。p95 表示约 95% 样本不超过该值，不是“最慢 5% 的平均值”。跨实例汇总时不能直接平均各实例的 p95，应使用适合聚合的直方图等方法。

## 怎么采到可信的客户端数据

流式接口经常用 SSE（Server-Sent Events，服务器发送事件）。一个事件可能只有角色信息，也可能包含多个 token。客户端脚本应忽略空内容事件，把首次非空文本到达标记为“首内容延迟”；这与精确模型首 token 时间有偏差，报告时要保留这个名称。

实验包的 `measure_stream.py` 会保存请求开始、首内容、结束及每个内容 chunk 的时间。它不会把 chunk 间隔伪装成 ITL；精确 token 指标应从支持相应观测的引擎获取。

## 改一个变量，再解释现象

第一组固定输出长度，增加输入长度；第二组固定输入，增加并发；第三组固定输入和并发，增加输出长度。每组先热身、重复测量，记录中位数和 p95，并保存引擎指标。

如果增加并发主要推高排队，先研究调度和容量；如果单请求 Prefill 随输入明显增长，继续看输入计算与缓存；如果只有长输出变差，则需要研究 Decode、KV 读取与同批请求的影响。不要一开始就随机调整所有参数。
''','ttft prefill decode tpot itl slo','vllm-metrics prometheus vllm-serving',['如果 TTFT 为 1.45 秒、总共输出 32 个 token，后续平均每个 40 ms，总耗时是多少？哪部分能由缩短输出改善？'][0],'总时间约为 1.45 + 31×0.04 = 2.69 秒。缩短输出减少后续生成时间，不能直接消除已经发生的排队和 Prefill。',['llm-observability'],lab='04-latency')

write('08-batching','更多请求，怎样一起执行？','通过请求进入和退出的过程，理解吞吐、等待和显存之间的关系。',r'''
如果一个请求每步只生成一个 token，GPU 可能没有获得足够大的计算任务。把多个请求放在一起执行，可以复用权重读取并增加并行工作量。但用户并不会同时到达，也不会同时结束。

## 静态批次为什么留下空位

设 A 要生成 5 步，B 只需要 2 步。如果一个批次必须等所有请求结束后才能接收下一批，B 完成后的处理位置可能闲着，新来的 C 还要排队。

<Term id="batch" /> 在执行步骤之间让完成的请求退出、新请求加入。不过“动态补位”是调度语义，不代表硬件每个位置真的有一块永久专属计算单元。真实引擎会重组张量和执行预算。

<Demo type="batch" client:visible />

关闭连续批处理，观察 t2 之后的 C；再打开它，保持容量为 2。比较同一请求从到达到开始执行的时间，而不只看每一步有几个彩色块。

## 增大 batch 的收益来自哪里

权重可以服务更多序列，一次执行包含更多计算，有机会提高硬件效率。但 KV Cache 通常随活跃序列数量增加；队列策略也可能为了组成更大工作量而增加等待。最终是否值得，取决于服务目标。

用小例子算：系统每秒完成 100 个请求，平均每个输出 50 token，输出吞吐约为 5,000 token/s。若输出长度翻倍，相同请求吞吐对应不同的 token 吞吐。比较时必须固定或报告长度分布。

## Prefill 和 Decode 为什么会互相影响

一个很长的 Prefill 可能占用执行时间，使正在 Decode 的请求等待更久。分块 Prefill（chunked prefill）把长输入拆成受预算限制的块，是平衡吞吐与输出间隔的手段之一。代价是调度更复杂，且分块策略需要结合引擎实现理解。

不能只看最大 batch size。还要关注每步 token 预算、活跃序列数、队列长度、KV 可用量和抢占。参数名称与语义随引擎版本变化，实际实验应记录完整启动配置。

## 怎样判断扩容前是否还有优化空间

先画出吞吐与并发的关系曲线，再画同一组实验的 TTFT 和 ITL 分布。如果吞吐已趋于平台，而尾延迟继续增长，更多并发可能主要在制造等待。这里的拐点只能从具体负载测得，不能凭一个 GPU 利用率阈值决定。

> 教学取舍：本页模拟器故意不建模 Prefill 和显存。它帮助观察补位行为；真实实验必须同时记录这些资源，否则可能得出“补位永远无代价”的错误结论。

## 一个排队直觉

稳定系统中，平均在系统内的请求数、平均到达率与平均停留时间存在 Little 定律关系：$L=\lambda W$。这里要求观测窗口和系统边界一致，并处于适合该关系的稳定条件。流量突发和过载时，不能用短窗口结果草率做容量承诺。
''','batch throughput kv-cache ttft itl','vllm vllm-metrics','连续批处理是否意味着并发越高越好？','不是。它减少某些等待空隙，但更高并发消耗 KV、增加资源竞争与排队。应联合比较有效吞吐、首 token 和输出间隔，并根据 SLO 选择工作点。',lab='04-latency')

write('09-kv-memory','KV Cache 为什么会吃满显存？','先算单个 token 的存储，再理解并发、分组查询和分页。',r'''
模型权重加载成功，只说明固定参数放得下。真正接入用户后，每个活跃序列还会留下自己的上下文状态。显存预算中的这部分会随请求变化：它就是 <Term id="kv-cache" />。

## 先数一个 token 保存多少数据

对于普通全注意力 Transformer，假设有 $L$ 层、每层 $H_{kv}$ 个 K/V 头、每个头维度 $d$，每元素占 $s$ 字节。每 token 的缓存数据量约为：

$$B_{token}=2LH_{kv}ds$$

系数 2 代表 K 和 V。若有 N 个等长活跃序列，每个保留 T 个 token，则 $B=2LH_{kv}dsTN$。变长时应把 TN 替换为各序列保留 token 数之和。

例如 32 层、8 个 KV heads、维度 128、BF16 每元素 2 字节：每 token 131,072 字节，即 128 KiB。单序列 8,192 token 约 1 GiB，8 个这样的序列约 8 GiB。这里使用二进制 GiB，不要与十进制 GB 混用。

<Demo type="kv" client:visible />

## 缓存中到底是什么

<figure className="knowledge-image"><img src={asset('images/kv-cache.webp')} alt="历史 K/V 保留，新 token 追加自己的 K/V，新查询读取缓存参与注意力计算" loading="lazy"/><figcaption>原创知识图：旧 K/V 被复用，新位置继续追加。Q 用于查询，不属于历史 K/V 缓存；缓存不是最终回答。</figcaption></figure>

普通自回归生成中，每推进一个位置，对应 K/V 会进入后续上下文。实现中的缓存槽位分配时机和 token 计数可能存在一步差异，容量规划要以引擎实际定义为准。

## 为什么实际占用不等于公式

公式只数理想 K/V 元素。实际引擎还需要分页块、对齐、元数据和临时缓冲。总显存还包括权重、激活、执行图、通信缓冲以及其他进程。降低 KV dtype 也不保证总显存减半，因为只有 KV 那一项变了。

滑动窗口注意力可能只保留限定范围；混合模型不同层的缓存形态可能不同；TP 对 KV 的分片或复制又受头数与实现约束。不能把公式作为所有模型的万能计算器。

## 分页解决的是组织方式

设每页容纳 4 个 token，一个长度 10 的序列需要 3 页，最后一页只用了 2 个位置。逻辑连续的序列可以映射到不连续的物理块，避免为了未来长度预留巨大连续空间，也方便复用与回收。

分页并没有消除每 token 的 K/V 本身，也没有让碎片变成零。页过大可能增加末页浪费；页过小又增加管理开销。PagedAttention 的关键是让注意力计算适配这种块映射，而不是简单调用操作系统换页。

## 缓存压力如何传到用户

<Slide id="llm-observability-14" minute="讲义把 KV 饱和、排队或抢占、TTFT/ITL 退化连成一条因果链。即使 GPU 忙碌比例不高，缓存容量也可能先成为限制。" />

你应同时看活跃序列、缓存占用、排队和抢占，而不是只看显存总量。引擎可能提前预分配内存池，因此 `nvidia-smi` 中占用不变不代表 KV 使用量不变。下一节会区分预留容量、已用块和能够复用的块。
''','kv-cache tensor hbm ttft','pagedattention vllm-metrics attention','一个模型有 32 个查询头、8 个 KV 头，估算 KV 时应该乘哪个？为什么分页后仍会占显存？','应使用 8 个 KV 头，查询头可分组共享 K/V。分页改变逻辑到物理块的组织并减少某些浪费，但不能消除实际 K/V 数据和元数据。',['llm-observability','tiered-kvcache'],lab='05-cache')

write('10-prefix','相同的开头，能省掉多少计算？','理解前缀匹配、缓存键与测量口径，避免把命中率当作最终收益。',r'''
两个人都向模型提交同一份长文档，只在末尾提出不同问题。前半段 token 完全相同，模型为它们计算的历史 K/V 是否可以共享？这就是前缀缓存最直接的应用。

## 什么才算相同前缀

匹配依据通常是实际 token 序列及影响计算的必要上下文，而不是肉眼看起来相似的文字。系统提示、chat template、模型 revision、适配器或多模态输入不同，都可能改变可复用结果。

设请求 A 是 `[a,b,c,d,x]`，请求 B 是 `[a,b,c,d,y]`。前四个位置可能复用，末尾分别处理。如果 B 改成 `[a,b,z,d,y]`，不能直接把第四个位置当作原来的 d：因果表示依赖此前上下文。

## 块与前缀链

实现通常以块为单位管理，完整块更容易参与共享。一个块的身份可能依赖之前块的身份，因此“块里文字相同”未必足够。最后一个未满块如何处理，以及不同 attention 类型的支持，必须对照引擎版本。

<Diagram id="prefix" caption="共享前缀形成分支；改动较早位置会改变后续上下文，而不仅影响改动的那个 token。" />

前缀缓存主要节省可复用输入的计算，不会自动消除新 token 的生成，也不保证网络加载免费。vLLM 官方 APC 文档明确讨论其适用范围与限制。

## 命中率至少有两种口径

按请求算命中：100 个请求里 90 个有任意复用，命中率 90%。按 token 或字节算命中：实际省下多少输入工作量？一个只命中 16 token 的长请求，与命中 16,000 token 的请求，不能只用“都命中”一笔带过。

进一步还需要看命中后的时间收益和缓存占用。命中很高但每次跨网络搬运巨大 KV，可能无法达到预期 TTFT。

## 怎样设计不自欺的 A/B 实验

准备三个负载：完全相同长前缀、相同长度但不同内容、前缀中段改变。分别在开启与关闭缓存的条件下测量。每组记录冷缓存与热缓存，随机交错顺序，保持并发和输出长度一致。

比较 TTFT、输入计算量、缓存查询与命中、显存和吞吐。不要用“第二次比第一次快”就断言缓存起作用，第二次也可能只是完成了编译热身。

## 多租户边界也是正确性的一部分

复用状态时要明确模型、适配器及租户隔离边界。不能为了提高命中，把本来不应该共享的上下文混在一起。具体隔离能力应来自引擎支持与服务设计，不在客户端自行拼凑一个不完整的缓存键。

下一节把同一个问题延伸到更长的 Agent 会话：缓存需要保留多久，放在哪种介质，什么时候应该直接重算？
''','kv-cache token prefill ttft','vllm-prefix pagedattention','两条输入在中间修改了一个 token，后面文字完全相同，后半段 K/V 可以直接复用吗？','通常不能。因果模型后续表示依赖此前前缀；需要匹配的不只是块内文本，还包括相关前缀与模型计算条件。',lab='05-cache')

write('11-tiered-cache','缓存放在哪里，才真的划算？','把 Agent 会话、保存成本、加载带宽和重算代价放到同一张账上。',r'''
普通对话的一次请求很短，Agent 任务却可能跨越多轮模型调用和工具等待。进程暂时没在生成，不代表上下文已经没有价值。缓存生命周期需要从单请求上移到整个会话。

## 为什么最近使用不等于最有价值

LRU（Least Recently Used，最近最少使用）优先淘汰最久未访问的数据。但前缀链的根块虽然暂时没被最近步骤触碰，可能仍是后续复用的必要条件；一个刚结束的会话即使刚访问过，保留它也未必有未来收益。

会议中的 UCM 案例把位置与会话生命周期纳入策略。根据本地妙记约 00:09:48–00:12:07，proxy、dispatcher 与健康处理负责决定块的位置和生命周期。这是讲者的架构案例，不代表所有缓存系统都必须采用相同算法。

## 建立加载与重算的比较

设需要加载 B 字节 KV，有效带宽为 W，固定请求和传输开销为 O，重新计算耗时为 R。简化条件下，加载时间 $T_{load}=B/W+O$。只有加载节省的时间大于其他代价，缓存才对当前目标有益。

例如 4 GB 数据经 8 GB/s 有效路径加载，再加 20 ms 开销，总计约 520 ms。若重算只需 300 ms，即使缓存命中，加载仍可能更慢；若重算要 2 秒，情况则不同。这是教学假设，没有计入与在线请求争抢带宽。

<Demo type="cache" client:visible />

## 分层不只是逐级复制

GPU 内存速度快但昂贵；主内存可以扩容却需要设备传输；SSD 和远端存储提供更大容量，但加载性能取决于访问粒度、并发、队列和介质。系统可以采用分层副本，也可以在多个存储池间分配；不能默认每一层都包含下层完整拷贝。

<Diagram id="cache-tier" caption="缓存管理决策包含位置、生命周期与传输；加载失败时仍需明确重算或失败的路径。" />

会议中提到将小 IO 聚合，并让 IO、复制和设备传输重叠。理解这个机制之前，要区分**带宽**与 **IOPS**：大连续传输可能容易吃满带宽，大量很小的访问则可能被每次操作开销限制。

## 怎么看待会议中的数字

讲者报告过约 99% 命中率、较大的容量需求和写盘量下降。现有记录缺少完整 trace、块大小、介质、冗余和窗口条件，因此本站保留其为讲者报告，不把数字写成普遍结论，也不在模拟器中伪造“复现 UCM”。

## 设计自己的缓存评估

保存每次访问的 session ID、块前缀关系、大小、时间、加载时间、重算估计和会话结束事件。对比 LRU、前缀感知与会话释放时，至少同时看字节命中、可复用完整前缀、TTFT、写放大和总容量。

会话结束事件可能丢失，所以释放策略还需要超时和回收机制。存储故障时要避免请求无限等待；缓存是加速路径还是必须依赖的状态，也必须事先说清楚。
''','kv-cache bandwidth ttft','vllm-prefix vllm-pd','缓存命中率 99%，为什么仍然可能比直接计算慢？','命中只说明数据存在，加载还要消耗 IO、网络和设备传输时间，并可能争用在线带宽。应比较加载总成本与重算成本，结合字节口径、时延和并发判断。',['tiered-kvcache'],lab='05-cache')

write('12-disaggregation','为什么把 Prefill 和 Decode 分开？','理解资源隔离的收益，以及随之增加的 KV 传输、路由和失败处理。',r'''
同一台推理实例既处理长输入，又持续生成短步骤。Prefill 和 Decode 的工作形态不同，混在一起可能互相干扰。PD 分离尝试让两类工作由不同实例负责，但它不是没有代价的“升级模式”。

## 一次分离请求怎样流动

入口先选择 Prefill 实例，完成输入处理后，将后续生成需要的 KV 和关联信息交给 Decode 实例。Decode 实例继续生成并返回流式输出。路由层需要知道这是一条逻辑请求，即使它经历了多个执行位置。

<Diagram id="pd" caption="PD 分离增加一个 KV 交接边界；控制信息、缓存数据和输出流各有职责。" />

**KV connector** 是引擎用于接入缓存传输/存储机制的集成点，不是一个保证所有模型和并行组合都兼容的万能插头。vLLM 文档仍将相应功能标为实验性，配置必须跟随明确版本与兼容矩阵。

## 哪些情况下可能值得

长 Prefill 干扰 Decode，或者两类工作需要独立扩缩容时，分离提供了新的控制空间。可按不同资源形态部署并分别排队。但如果请求很短、单机就能高效处理，额外路由和传输可能占比过高。

一个简单比较是：原来被干扰的等待时间是否大于新增加的 KV 交接、调度与网络时间？不要只比较某一段 GPU kernel，因为用户感受到的是整条链路。

## 数据格式和并行布局必须一致

Prefill 与 Decode 的模型权重、KV dtype、布局和分片方式需要兼容。TP 大小不一致时，KV 如何重新分布不能想当然。位置编码、适配器、混合注意力等条件也会影响正确交接。

启动成功只说明进程活着。至少需要端到端正确性、长短输入、不同并发，以及取消和超时测试，才能说明分离链路可用。

## 失败在哪一侧发生

Prefill 完成但 KV 传输失败，应该重试传输、重算，还是终止请求？Decode 实例已预留缓存却一直没收到数据，如何清理？客户端取消之后，两个实例都必须结束相应状态，不能只断开网关连接。

这也是观测必须贯通的原因：一个 trace 可以包含 Prefill span、KV handoff span 和 Decode span；GPU 指标仍需按设备和时间窗口解释，不能将所有指标简单归到一个请求。

## 实验怎样开展

先保留单实例基线，再部署官方同版本 connector 示例，固定模型和负载。比较 TTFT、后续输出间隔、有效吞吐、传输字节和失败率；分别测试短输入、长输入和混合负载。

实验包提供版本核对与验收步骤，并指向已锁定版本的官方示例。因为不同 connector 对硬件和布局条件不同，本课程不制造一个看起来通用、实际上不兼容的配置。
''','prefill decode kv-cache tp trace','vllm-pd vllm-distributed','PD 分离能让 Prefill 和 Decode 独立扩容，为什么仍需要单实例基线？','分离增加路由、KV 传输、资源预留和故障清理成本。只有在相同负载与目标下，收益超过这些代价，才有证据支持采用。',['tiered-kvcache','llm-observability'],lab='06-distributed')

write('13-parallelism','模型和请求，应该怎样分给多张卡？','从复制、按层切分、按张量切分和专家路由四种方式理解并行。',r'''
“用了 8 张 GPU”没有说明它们怎么合作。可能是 8 份独立模型，也可能是一个模型切成多份。两者在显存、吞吐、延迟和故障影响上完全不同。

## 先用两个问题选方向

第一，单卡是否装得下目标模型与上下文预算？第二，目标是缩短单请求时间，还是接更多独立请求？如果单卡可运行且主要需要吞吐，多副本可能更直接；如果模型装不下，就需要考虑模型切分。

| 方式 | 切分对象 | 主要通信 |
|---|---|---|
| DP / 推理副本 | 不同输入请求 | 推理副本通常不需要逐层同步；训练需要梯度同步 |
| TP | 同一层的权重与张量计算 | 层内中间结果集合通信 |
| PP | 不同层或阶段 | 阶段间传递激活 |
| EP | MoE 专家 | token dispatch 与 combine |

<Term id="dp" />、<Term id="tp" />、<Term id="pp" /> 和 <Term id="ep" /> 可以组合，但组合越多，布局与调度越复杂。

## 一个线性层怎样做 TP

设 `Y = XW`，W 形状为 `[4,6]`。将 W 按列分为两个 `[4,3]` 的部分，每卡计算自己的三个输出维度，之后按需要拼接。如果按行分，则 X 和 W 沿对应维度切分，得到的是部分和，需要相加。

这个例子说明：通信类型由数学分解决定。不能先说“用 AllReduce”，再忽略结果究竟应该拼接还是求和。

## PP 为什么有空泡

一个请求先经过前面层，再到后面层。流水线需要足够的微批次填充，否则某些阶段在等输入或等下游。各阶段计算时间不均匀，也会由最慢阶段限制吞吐。单请求交互式生成与大批训练的流水线利用方式并不相同。

## MoE 中 token 为什么会跨卡

Mixture of Experts 用路由器为 token 选择专家。专家分布在不同设备，输入需要 dispatch 到目标专家，计算后再 combine。专家负载不均匀、跨节点发送量和小消息延迟都会影响性能。

<Slide id="advanced-nccl-23" minute="NCCL 教程用 MoE 展示 dispatch、FC1、FC2、combine 的完整顺序，后续再讨论如何把通信和计算细化到 tile 并重叠。" />

## 把拓扑带进决策

频繁层内通信通常希望利用较快的设备互连。跨节点网络与节点内互连的延迟、带宽和可达性不同，因此“相同 GPU 数”不等于相同系统性能。

实验时固定全局负载和模型配置，记录每卡峰值显存、实际通信、吞吐与尾延迟。GPU 数翻倍后速度不翻倍，可能来自通信、负载不均或模型本身并行度不足；这需要测量，而不是直接归因于某个框架。
''','dp tp pp ep nccl','vllm-distributed nccl-api','线性层按输出列切分和按输入行切分，为什么需要不同的通信？','按输出列切分得到不同输出分量，通常需要拼接或让后续层消费分片；按输入行切分得到同一输出的部分和，必须归约求和。',['advanced-nccl'],lab='06-distributed')

write('14-collectives','多张 GPU 怎样交换结果？','用小数组理解集合通信，再解释拓扑、rank 和测量口径。',r'''
分布式程序中，“交换数据”有多种准确含义。把所有数组拼起来、把数组按元素求和、把不同片段分给不同参与者，是不同的操作。

## 从两张卡的数字开始

<Term id="rank" /> 0 保存 `[1,2]`，rank 1 保存 `[3,4]`。<Term id="allreduce" /> 求和后，两者都得到 `[4,6]`。AllGather 后，两者得到包含 `[1,2,3,4]` 的集合结果；Broadcast 则由一个源把自己的值发送给其他参与者。

ReduceScatter 可以理解为先对全体数据做归约，再让每个 rank 留下指定的部分。AllToAll 则让每个 rank 分别向其他 rank 发送不同分块，常见于专家路由。

<Slide id="advanced-nccl-9" minute="讲义先用 Host API 的 send/recv 与 collectives 建立基线，再解释 Device API 为什么需要更细粒度通信。" />

## 每个参与者必须遵守同一协议

集合通信需要参与者在调用、数据类型、元素数量和顺序上满足匹配要求。如果一个 rank 提前走入下一次 collective，另一个仍在前一次，可能挂起或出错。局部日志看似正常，不代表整个组达成一致。

点对点发送接收也不是随便成对写就安全。需要考虑执行顺序、缓冲生命周期和同步。复杂程序应从可验证的小通信模式开始。

## 单机和跨节点的路径不同

<Term id="nccl" /> 根据能力和拓扑使用不同通信路径。节点内可能涉及 PCIe、NVLink 和 P2P；跨节点涉及 NIC、网络和 <Term id="rdma" />。GPU 与 NIC 是否可直接传输、是否需要主内存中转，要看硬件、驱动和配置。

<Diagram id="topology" caption="准确拓扑示意：NVLink 连接 GPU；NIC 通过 PCIe 连接主机拓扑，跨节点由网络连接 NIC。" />

## 怎么读带宽结果

算法带宽常按用户数据量除耗时计算；总线带宽会根据通信算法或 collective 数据流量作换算。两者不是同一个数，不能混合比较。小消息更容易受启动延迟影响，大消息更容易表现带宽约束。

记录 GPU 型号、节点数、网卡和链路、消息大小、NCCL/CUDA/驱动版本、热身与重复次数。改变 rank 数时，还要考虑同一 collective 的实际传输模式发生变化。

## 排查挂起时先缩小问题

先确认所有 rank 都能看见正确设备，再测试主机/DNS 发现、单对节点连通性，然后做小规模 collective。不要让一个复杂 MoE 模型承担网络连通性测试的全部责任。

NCCL 教程源码包含两 rank LSA 和 GIN barrier 示例。主线实验先做传统 AllReduce 正确性验证，进阶专题再进入设备侧同步，避免还没建立 collective 基线就直接调试融合 kernel。
''','rank allreduce nccl rdma','nccl-api nccl','两张卡执行 AllGather 后，结果应该是逐元素求和吗？如果某个 rank 没调用同一 collective 会怎样？','AllGather 收集各参与者的数据，不自动求和。集合通信需要全体参与者遵守匹配协议；缺失或顺序不一致可能导致等待、错误或超时。',['advanced-nccl'],lab='07-nccl')

write('15-nccl','让通信和计算同时推进','理解 Device API 的四个对象，以及分块流水线为什么能缩短等待。',r'''
传统执行方式可以先收齐一整块输入，再启动计算。但如果前半块已经到达，计算却仍在等后半块，系统就失去了重叠机会。NCCL Device API 让自定义 GPU kernel 直接组合通信原语，尝试缩小这样的等待。

## 先看对象各管什么

Device communicator 提供通信上下文与能力；window 指定注册的内存范围；team 决定参与的 rank 子集；coop 决定共同执行操作的线程范围。team 和 coop 都是“组”，但前者组 rank，后者组线程，不能混淆。

<Slide id="advanced-nccl-14" minute="四个对象把参与者、内存、线程协作与 transport capability 分开。Host 先声明资源和能力，device kernel 接收相应句柄。" />

## Symmetric memory 不要求地址数值相同

两个进程虚拟地址可以不同，只要注册窗口具有可对应的布局与偏移，就能根据 peer 和 offset 定位相应数据。地址看起来一样也不自动代表是同一块可访问物理内存。

<Slide id="advanced-nccl-20" />

<Term id="lsa" /> 用于具备 load/store 可达性的对端内存路径；Multimem 还要求相应硬件多播能力；<Term id="gin" /> 允许 GPU 发起网络通信。它们是不同能力，实验前必须查询支持条件。

## 从完整缓冲区到 tile

tile 是大张量中的一个计算/传输小块。以 MoE 为例，某块 dispatch 完成后就可以开始该块计算，同时后续块继续到达。收益来自阶段重叠，而不是简单地把某次复制命令换了名字。

<Slide id="advanced-nccl-24" steps={['上方顺序：Dispatch 完成后才进入 FC1，阶段间存在等待。','下方按 tile 推进：tile 0 可以先算，后面的块继续通信。','重叠需要正确同步和独立资源；图示不保证任何负载都有相同收益。']} />

<Demo type="overlap" client:visible />

若 n 块每块传输 t、计算 c，完全串行约为 `n(t+c)`；理想双阶段流水线约为 `t+c+(n−1)max(t,c)`。第一块需要填充，最后一块需要排空，不能直接把时间写成 `max(nt,nc)` 后忽略边界。

## 同步是正确性的组成部分

开始读某块之前，要确认写入数据已经对消费者可见。消费者结束前，生产者不能重用仍被读取的缓冲。设备上的操作发起、源缓冲可重用、远端数据可消费，可能是不同事件。

注册和创建 communicator 通常在热循环外摊销，但其生命周期必须覆盖实际工作。过早注销 window 或释放设备内存会破坏正在进行的传输。

## 版本不能混用

官方 NCCL 文档说明 Device API 从 2.28 开始提供，GIN 从 2.28.7 开始提供；后续版本增加能力并存在编译/运行时兼容条件。本教材进阶实验固定 2.31.2 并记录 CUDA 与硬件条件，不把会议中的 API 片段直接拼到任意系统上。
''','nccl rank lsa gin kernel','nccl','为什么“发送函数返回”不能总被解释为“远端已经可以读数据”？','不同接口的返回只保证特定完成层级。源缓冲可重用、传输已到达和远端可见可能需要不同的同步或 signal 协议；必须依据具体 API 的语义建立交接。',['advanced-nccl'],lab='07-nccl')

write('16-gpu-scheduling','Kubernetes 怎样给 Pod 找到 GPU？','从整数资源请求走到设备健康、共享和拓扑约束。',r'''
写下 `nvidia.com/gpu: 1`，看起来像申请一个 CPU。区别在于 GPU 是具体设备：不同型号有不同容量与能力，设备可能故障，也可能通过硬件或软件方式共享。

## 传统扩展资源如何表达需求

Device Plugin 向 kubelet 报告设备并参与分配，使节点能够暴露类似 `nvidia.com/gpu` 的扩展资源。调度器首先基于节点可分配资源等条件选择节点，具体设备的使用还涉及节点侧机制。

```yaml
# 典型 device-plugin 路径中的容器片段
resources:
  limits:
    nvidia.com/gpu: 1
```

这个整数表达“需要一个资源单位”，但资源单位是否代表完整 GPU、某种切分或共享配置，要看驱动的资源定义。不能看到 1 就推断拥有一张物理卡的全部独占能力。

## 共享有不同语义

MIG（Multi-Instance GPU）在支持的硬件上创建隔离的设备实例；time-slicing 让工作轮流共享 GPU 时间；MPS 提供多进程执行相关机制。它们在内存、故障隔离和性能干扰上并不等价。没有具体配置时，不宣称共享后每个租户获得稳定同等性能。

## 节点空闲不代表拓扑合适

<Term id="numa" /> 描述不同内存和设备访问路径的代价差异。一个模型需要 GPU 与 NIC 协作，如果它们分布在远端拓扑，数据可能经过额外路径。任务能被调度和任务被高效调度是不同的标准。

<Diagram id="topology" caption="GPU、NIC 与 CPU 插槽的位置影响通信路径；拓扑约束需要与实际驱动属性对应。" />

## 工作组需要一起考虑

分布式服务可能需要同时获得多张 GPU。只保证每个 Pod 分别有资源，可能出现部分成员启动、其他成员长期排队。此时资源被占用却没有完整模型可以服务。

资源预留、组调度与启动编排分别解决不同阶段的问题。LWS 管理组生命周期，不应被当作所有集群组调度策略的替代品。

## 从“多少个”走到“是什么”

<Slide id="kubernetes-dra-9" />

DRA 允许用结构化属性和声明表达更多需求，随后还需要分配、准备和状态管理。它并不让复杂硬件差异消失，而是把这些差异放在可以被选择和观测的接口上。

## 一个部署前的检查清单

列出模型需要的设备数量、单设备显存、共享限制、网络要求和失败影响。再查看集群实际暴露的资源名、驱动版本、健康与节点标签。资源名必须从集群查询，不能凭厂商名称猜测。

实验里先走传统资源路径完成可运行基线，再使用 DRA 实验对照。这样可以区分“应用本身跑不通”和“新的设备分配路径有问题”。
''','gpu pod numa dra lws','device-plugin dra','Pod 请求一个 GPU 资源单位，就能保证独占整张物理 GPU 吗？','不能。资源单位由设备插件或驱动配置定义，可能对应完整 GPU、MIG 实例或共享单位。必须核对实际资源语义和隔离能力。',['kubernetes-dra'],lab='08-kubernetes')

write('17-dra','一次设备请求经历了什么？','分清库存、声明、分配、绑定和节点准备，避免把对象创建当成资源可用。',r'''
传统设备请求往往先问“要几个”。<Term id="dra" /> 进一步允许询问“设备有什么属性、如何访问、是否准备好”。理解 DRA，最有效的方法是沿一次请求走完生命周期。

## 四类对象各自表达什么

**ResourceSlice** 是驱动发布的设备库存与属性。**DeviceClass** 提供设备选择规则。<Term id="claim" /> 表达请求并承载分配结果。**ResourceClaimTemplate** 用于为工作负载生成相应声明。

它们不是四份重复配置：库存由设备提供方维护，请求由使用方表达，类别连接两者。对象存在也不代表分配成功；有分配结果也不代表节点准备已完成。

<Slide id="kubernetes-dra-23" minute="讲义将 Kubernetes 的 Pod/Claim/调度绑定，与驱动的 ResourceSlice 发布、prepare/unprepare 和 CDI 注入分开。这条边界是读懂故障位置的关键。" />

## 跟着一次请求走

驱动先发布设备。用户创建或引用 Claim。调度过程结合节点约束和设备选择条件作出分配与绑定。节点 kubelet 需要时调用驱动准备资源；驱动可借助 CDI（Container Device Interface，容器设备接口）描述设备如何进入容器。容器结束后还涉及解除准备与回收。

<Demo type="dra" client:visible />

页面中的设备列表是教学数据，健康状态已被简化为布尔值。实际 DRA 健康、taint 和绑定条件各有版本与驱动语义，不能直接将模拟字段写入 YAML。

## 一个最小声明的形状

```yaml
apiVersion: resource.k8s.io/v1
kind: ResourceClaim
metadata:
  name: example-gpu
spec:
  devices:
    requests:
      - name: gpu
        exactly:
          deviceClassName: example-gpu-class
          allocationMode: ExactCount
          count: 1
```

`example-gpu-class` 是说明用途的名字，必须替换为驱动真实提供的 DeviceClass。完整实验会先枚举类别并用 API Server dry-run 校验。仅验证 YAML 能被解析，不能证明驱动有能力完成分配。

## 失败应该看哪一层

Claim 没有分配：查库存、类别规则、节点约束与资源竞争。Pod 已绑定但容器进不去：查 kubelet、驱动 prepare 和设备注入。运行后设备异常：查驱动状态和工作负载恢复策略。不要在不同阶段重复删除 Pod，导致证据丢失。

## 核心能力与高级功能分开核对

会议讲义同时出现 1.36、1.37 和 1.38 计划。实验以 Kubernetes 1.37 作为声明基线，高级拓扑、派生属性和驱动扩展逐项核对 feature gate；后续版本计划只放进进阶说明。

DRA 解决了表达与协作机制，但仍需要驱动提供准确库存、稳定状态和正确设备准备。集群规模变大后，ResourceSlice 与状态更新频率也会影响控制面负载。
''','dra claim pod numa','dra dra-features','Claim 已经有分配结果，为什么容器仍可能无法启动？','分配确定设备选择，节点准备仍可能失败，例如驱动无法建立访问、设备注入配置错误或绑定条件未满足。需要检查 kubelet 和驱动，而不是把分配与准备当作同一步。',['kubernetes-dra'],lab='09-dra')

write('18-lws','为什么 Pod Running 还不够？','把分布式启动看成一组共同条件，而不是若干独立的容器状态。',r'''
一个模型用 16 张 GPU 协作，只要一个 rank 的网络或驱动有问题，整个服务可能无法正常输出。等模型加载和通信初始化耗费很久后才发现故障，会浪费整组资源。

## LeaderWorkerSet 表达什么

<Term id="lws" /> 管理 leader 与 worker Pod 组成的工作组，一个组可以对应一个模型副本。组内发现、启动顺序和重启策略属于它要表达的生命周期语义；模型本身如何分片和通信仍需推理引擎配置。

先区分服务副本数与组大小。两个副本、每组四个成员，意味着两套工作组，而不是一个八成员模型。阅读配置时把 replica、group size、rank 和 TP/PP 分开。

## 为什么用 init container 检查

init container 在主要应用容器启动前执行，能够在相同 Pod 的设备、网络、存储和身份条件下阻断后续启动。独立 Job 可能落在不同节点，无法证明实际 placement 健康；sidecar 则未必天然阻断服务进程加载。

<Slide id="lws-preflight-4" minute="讲义比较 sidecar、startupProbe、独立 Job 和 Operator，强调检查环境与真实工作负载 placement 一致的价值。" />

## 从便宜检查到昂贵检查

先检查配置、挂载、驱动和设备可见性；再检查 DNS、对端发现与网络；然后执行成对带宽测试和全组 collective；最后做小规模模型 smoke test。每一层失败时都记录原因，避免昂贵步骤继续运行。

<Slide id="lws-preflight-10" minute="讲义按 OS/mount、GPU/NIC、fabric、collective/runtime 分层。妙记约 00:05:38 起提到旧驱动导致网络路径退化的案例，说明仅检查进程存活不够。" time="00:05:38 起" />

## 全组结论如何形成

检查需要确定 rank/world、对端地址、超时和共享 verdict。任意 rank 失败时，不能让其他成员带着局部成功进入服务阶段。退出码可以阻断当前 Pod，但全组恢复仍需要控制器和明确策略。

<Diagram id="preflight" caption="各 rank 完成本地与通信检查，形成全组结论；失败有预算，证据先保存。" />

## 重启为什么会变成问题

如果每次 init 失败都触发整组重建，可能陷入无限循环。日志丢失、故障节点继续被选中、资源持续消耗。需要最大重试预算、失败分类、证据保留和停止条件。

会议讲义中的 `maxGroupRestarts` 等形态属于相关提案讨论。教材使用 LWS 0.10.0 的已发布 API，并把这些字段作为提案分析，不擅自加入可执行清单。实际部署先用 `kubectl explain` 和 CRD schema 验证支持范围。

## 让一次故障产生下一次可用的知识

至少区分重试、重新调度和停止：短暂网络抖动可以有限重试；单节点硬件故障可能需要隔离与重新调度；模型版本不匹配应停止并修正配置。记录“检查名、rank、节点、开始结束、退出码和原因”，比只记录一个最终 failed 更有用。
''','lws pod rank nccl','lws lws-api lws-preflight',['为什么单独运行一个网络测试 Job 成功，不能证明即将启动的 LWS 工作组一定健康？'][0],'Job 可能使用不同节点、设备、网络、挂载和身份。需要在实际工作组条件下进行本地及全组检查，并确认所有 rank 满足相同协议。',['lws-preflight'],lab='10-preflight')

write('19-signals','指标、日志和追踪各回答什么？','从可回答的问题出发选择观测信号，而不是先堆满监控面板。',r'''
当用户说“今天比昨天慢”，你需要先知道慢在哪里、影响多少请求，以及能否找到同一次请求的相关证据。一个数字、一条日志、一张 trace 图分别提供不同视角。

## 三种信号不是互相替代

**Metrics** 是按标签与时间聚合的度量，适合观察趋势和报警。**Logs** 是离散事件记录，适合解释失败上下文。<Term id="trace" /> 描述一次执行的阶段和关联，适合看时间花在哪里。

| 想知道什么 | 优先信号 | 还需要什么 |
|---|---|---|
| 最近五分钟是否整体变慢 | 延迟分布指标 | 请求量和长度分布 |
| 一个请求卡在哪个阶段 | Trace | 完整传播与采样条件 |
| 某个 worker 为什么退出 | 日志与事件 | rank、Pod、节点关联 |
| GPU 忙但产出下降 | 引擎与硬件指标 | 时间窗口和负载解释 |

## Counter、Gauge、Histogram 的区别

Counter 是累计值，通常看一段时间内增长率；进程重启会重置。Gauge 是某时刻的值，例如队列长度。Histogram 将观测值累计进桶中，可用于估计分位数。

如果把累计 token 数直接画成速度，曲线会一直上升却回答不了吞吐。应该按时间计算增量。队列长度则应保留当前状态，不能当作永久累计计数。

## 从 SLO 决定该收什么

<Term id="slo" /> 必须说明观察对象、窗口、成功条件和排除规则。例如实验中定义“某个固定负载下，95% 请求首内容延迟小于 2 秒”，同时记录错误与取消。这里的 2 秒是练习目标，不是通用标准。

不要只统计成功请求：如果慢请求都超时退出，剩下请求的延迟可能变好，用户体验却更差。失败、超时和取消应有独立计数。

## 标签带来可检索性，也带来成本

模型、引擎实例、Pod 和 GPU 标识有助于定位。但把每个 request ID 或完整 prompt 放进指标标签，会导致高基数和存储压力。请求级标识更适合 trace 或日志，敏感输入也不应默认采集。

## 统一语义要固定版本

OpenTelemetry 提供 span、属性和指标的共同约定。GenAI 规范仍在演进并已迁移到独立仓库；不能把会议上的一个字段名当成永久 API。应用侧、引擎侧和采集器的版本及属性映射要记录在同一份契约中。

## 一份最小观测清单

先收请求数、错误数、排队、首 token/首内容、后续输出、输入与输出量、KV 使用和抢占。再接入 CPU、网络与 GPU 指标。每增加一个面板，都写出它能验证的假设；无法解释用途的面板暂时不加。

后续实验同时保留原始 `/metrics` 快照、查询表达式和时间窗口，使图表结论可复查。
''','trace slo ttft throughput','otel otel-genai prometheus vllm-metrics','为什么不能直接平均两台实例各自的 p95，作为整个服务的 p95？','分位数不是可线性平均的统计量，不同实例样本量和分布也不同。需要聚合原始样本或合适的直方图桶，再计算总体分位数。',['llm-observability'],lab='11-observability')

write('20-correlation','怎样从慢请求追到 GPU？','用传播上下文、资源标识和时间窗口连接证据，并保留归因边界。',r'''
看到一个很慢的请求和一张很忙的 GPU 图表，不能直接断言后者导致前者。两条证据需要同一时间范围、同一实例映射，以及能解释机制的中间环节。

## 先建立请求级关联

网关创建或接收 trace context，并向下游传播。引擎生成代表排队、Prefill 或执行等阶段的 span 时，应保留父子或关联关系。`traceparent` 是常见的上下文传播格式，但只有各层实际支持并正确接入，链路才完整。

<Slide id="llm-observability-11" />

一条客户端请求也可能产生重试、拆分或异步工作，不能假设天然是一条线性父子树。span links 可以表达某些非树形关联；具体采用方式应跟随实际 instrumentation。

## 再建立资源级关联

从服务实例映射到 Pod、节点和 GPU UUID。GPU 序号 0 在不同容器或节点中可能指向不同物理设备，长期关联更适合使用稳定设备标识。容器重启后，Pod UID、进程和时间范围也可能变化。

<Diagram id="correlation" caption="请求级追踪与设备级指标通过实例、设备标识和时间窗口关联；共享资源的消耗不能强行精确归属。" />

## GPU 指标通常不是请求专属数据

连续批处理中，多请求共享一次 kernel 或一段设备执行。GPU utilization、HBM 活动和网络吞吐往往是时间窗口聚合值。它们能支持“这个时间段设备处于什么状态”，却未必能支持“这个请求消耗了多少 GPU 时间”的精确归因。

如果没有真实设备级追踪支持，不要制造一个看起来很完整的 per-request GPU span。诚实的空白比错误精度更有用。

## 用同一时间窗口问问题

请求 10:00:03 到 10:00:05 慢，硬件图表若按一分钟聚合，短暂尖峰可能被稀释。不同机器时钟偏差也会让事件顺序看起来错乱。收集器抓取周期、trace 采样率和时间同步都影响可见性。

观察缺失不等于没有发生：低采样 trace 可能漏掉故障，指标 scrape 失败也可能让曲线断裂。排障笔记应记录这些限制。

## 把现象串成可验证链

例如：用户 TTFT 上升 → 对应 span 的 queue wait 增加 → 同时引擎 KV 使用接近上限且发生抢占 → 检查活跃请求长度分布变化。最后一步需要负载数据支撑，不能因为两个曲线同时上升就直接宣告根因。

<Slide id="llm-observability-15" minute="讲义的诊断范式是现象、trace 阶段、相邻层指标、根因与调优动作。地图和标签只有进入具体检查步骤，才能变成排障能力。" />

## 留下一份可复查记录

每次调查保存症状、时间窗、受影响对象、候选假设、证据、反证和下一步。改变参数后重复相同负载，确认目标指标改善且没有把问题转移到错误率或其他请求上。
''','trace ttft kv-cache gpu','otel otel-genai vllm-metrics','为什么同一秒 GPU 很忙，不能证明某个慢请求的全部时间都花在 GPU 计算？','请求可能在排队，GPU 可能服务其他请求或执行通信；指标也是采样窗口聚合。需要请求阶段、资源映射与机制证据联合判断。',['llm-observability'],lab='11-observability')

write('21-diagnosis','把三个性能故障查清楚','从首 token 上升、忙而低产出、KV 饱和三类现象练习诊断。',r'''
排障的目标不是尽快猜中一个参数，而是逐步排除错误解释。下面三个案例都先给现象，再给需要补充的证据。不要跳过反证，否则很容易把巧合当根因。

## 案例一：请求量没变，首 token 变慢

“请求量没变”没有说明输入长度是否变了。先比较输入 token 分布、并发、输出长度和到达节奏。相同平均 QPS 可能具有不同突发程度。

<Slide id="llm-observability-12" />

若 queue wait 上升而单请求 Prefill 没变，检查容量、调度和突发。若 Prefill 随长输入增加，检查缓存复用和计算预算。若只有客户端首内容变慢，引擎指标稳定，检查网关缓冲、网络与客户端。

反证很重要：把同一请求单独发到空闲实例，如果仍慢，纯排队假设就不充分。保留冷启动与稳定状态的区别。

## 案例二：GPU 很忙，token 产出却低

GPU utilization 描述忙碌采样，不等于有效模型 FLOPs 利用率。通信等待、内存访问、低效率 kernel 或不合适的批次都可能与高忙碌同时出现。

<Slide id="llm-observability-13" />

先确认吞吐口径和负载是否可比，再检查 SM/Tensor 活动、内存带宽、通信与执行时间线。改变 TP 大小后若通信占比显著增加，不能只看单卡显存下降就判定优化成功。

反证：如果关闭压测后仍有高占用，检查是否有其他进程；如果设备指标正常但业务吞吐下降，检查应用端重试、限流和输出计数。

## 案例三：KV 饱和引发连锁反应

更多活跃长序列占用缓存，调度器可能排队或抢占，随后 TTFT 和输出间隔变差。显存已被预分配时，设备总占用可能几乎不变。

检查引擎 KV 块使用、等待队列、抢占次数和长度分布。短期可限制并发或长度，但这改变了服务能力；长期可能需要更好的缓存策略、路由和容量设计。

## 三个案例共用的记录表

| 字段 | 应该写什么 |
|---|---|
| 症状 | 哪个用户指标、哪个窗口、哪些请求 |
| 假设 | 例如排队、长输入、网络退化；一次一个 |
| 验证 | 需要哪条 trace、哪个指标或对照实验 |
| 反证 | 出现什么结果就放弃该假设 |
| 动作 | 改哪个参数，预期改变哪一阶段 |
| 回归 | 吞吐、延迟、错误与资源是否一起检查 |

## 练习：不要把调优写成随机搜索

用实验包的负载脚本制造长输入与并发变化，先写预期，再运行。另用受控错误版本验证发布回滚，不在未经授权的共享生产环境做故障注入。

GPU 网络降级实验需要专用测试环境和明确恢复步骤。若无法运行，保存计划与未验证状态；不要拿一张模拟曲线充当结果。
''','ttft kv-cache throughput trace','vllm-metrics prometheus nccl','请求 QPS 没变，为什么负载仍可能变重？','输入/输出长度、并发、到达突发、模型与缓存命中分布都可能改变每请求工作量。必须固定或记录这些维度，QPS 只是其中一项。',['llm-observability'],lab='11-observability')

write('22-canary','怎样让坏版本少影响一些用户？','从流量控制与分析结果理解渐进交付，明确可自动化的边界。',r'''
一个新推理版本通过了单元测试，不代表它在真实长度分布、并发和设备条件下不会退化。<Term id="canary" /> 先暴露少量流量，再用证据决定是否继续。

## 副本比例与流量比例不同

一个新 Pod 加九个旧 Pod，在某些负载均衡条件下可能近似 10% 流量，但连接复用和请求差异会造成偏离。若需要精确路由权重，应使用与 Rollouts 集成的流量路由机制，并观测实际请求分布。

本教材综合项目在服务版本与网关路由层进行 Canary；LWS 负责模型工作组生命周期。两个控制器不应同时争抢同一个资源的期望状态。

## AnalysisTemplate 和 AnalysisRun

AnalysisTemplate 描述采样指标、间隔与成功/失败条件。AnalysisRun 是一次实际执行，结果可能是 Successful、Failed 或 Inconclusive。成功继续、失败终止或回滚、不确定暂停的行为需要结合 Rollout 配置理解。

<Slide id="self-healing-rollouts-12" />

判断规则至少同时看绝对服务目标和相对基线。旧版本也可能已经退化，如果只要求“新版本不比旧版本差”，就可能让两个坏版本一起通过。

## LLM 服务的分析不能只有 HTTP 200

请求返回成功但 TTFT 严重退化，仍然会伤害用户体验。应根据任务选择错误、延迟、输出速度、有效吞吐以及必要的质量检查。模型回答质量没有通用的单一阈值，测试集和评估规则需要明确。

对于长流式请求，回滚还需要考虑在途连接：停止新增流量与强制结束已有流不是同一个动作。优雅终止和 drain 时间要与最长请求和超时策略匹配。

## 监控系统坏了怎么办

指标查询超时、无样本、窗口不齐，都不能自动解释为“新版本正常”。为这些状态设置明确结果和预算，保留人工判断入口。分析系统本身也需要可观测性。

<Diagram id="rollout" caption="发布由可审计的分析结果驱动；无数据和分析失败有明确分支，修复进入正常评审。" />

## 一个可复查的发布实验

固定旧版，部署一个可控制错误率或延迟的新版本。逐步放量并记录实际流量比例、分析窗口和状态变化。验证失败后新流量回到稳定服务，再检查在途请求如何结束。

清理实验对象前保存 Rollout、AnalysisRun 和网关事件。这样能回答“为什么回滚”，而不只是“回滚了”。
''','canary slo gitops lws','rollouts rollouts-release','监控查询超时，能否当成零错误率让 Canary 继续？','不能。没有数据不等于没有错误。应映射为明确的错误或不确定结果，按配置暂停或失败，并保留查询错误与分析记录。',['self-healing-rollouts'],lab='12-rollouts')

write('23-agent-repair','Agent 能参与修复到哪一步？','把模型的诊断能力放进有证据、可测试、可回滚的工程闭环。',r'''
Agent 可以同时读取日志、指标和配置，提出故障解释或生成修改。但“写出了看似合理的原因”和“系统可以安全推进发布”之间，仍有一段需要工程验证的距离。

## 会议案例的闭环

收集器分别读取 stable 与 canary 数据，分析 Agent 比较异常，评分 Agent 检查结论，再由发布系统消费一个明确结果。可修复的问题生成 PR，复杂问题留下 issue 或人工处理入口。

<Slide id="self-healing-rollouts-20" minute="现场演示将并行数据收集、分析和评分接到 Argo Rollouts 的分析插件。发生错误后先回滚，再让修复进入代码评审与 CI。" />

评分 Agent 提供额外检查，但两个模型同意并不等于事实正确。它们可能共享同样不完整的上下文、误读同一份日志，甚至被相同的外部文本影响。

## 先定义确定性的输出契约

分析结果应包含状态、证据引用、置信说明、建议动作和错误信息。发布门禁消费有限状态，而不是自由文本里出现“看起来不错”就继续。超时、解析失败、证据不足应有明确分支。

```json
{
  "decision": "inconclusive",
  "evidence": ["metrics-window-2026-09-13T10:00Z"],
  "reason": "canary 样本不足，无法比较尾延迟",
  "proposed_action": "暂停放量并补充采样"
}
```

这是教材自定义的分析协议示例，不是 Argo Rollouts 原生字段。适配器需要显式映射到发布系统支持的结果，并验证输出结构。

## 给工具设置具体边界

读取日志和指标与写集群配置分开。修复工作在分支中完成，保留原始证据、diff、测试和失败记录。<Term id="gitops" /> 控制器依然从审核后的声明驱动部署。

外部日志和 issue 内容属于被分析的数据，不能变成给 Agent 的高优先级指令。密钥不进入日志和模型上下文；工具权限只覆盖实验所需资源。

## 四个必须测试的负例

监控后端不可用；stable/canary 时间窗口不一致；分析与评分结论相反；模型提出的修改通过语法检查却破坏行为。每个负例都应阻止未经验证的推进，而不是依赖模型临场判断。

模型版本与提示词也影响结果，需要与代码一起记录。重复相同证据得到不同建议时，应观察不确定性，而不是只保留最符合预期的一次。

## 什么叫完成了修复

修复不是生成一个 PR 就结束。它需要能复现的问题、解释机制的修改、能失败再通过的测试、重新发布的结果，以及确认没有新退化的观察窗口。

综合实验保留人工评审边界，同时验证自动回滚不依赖 Agent 自由文本。这样即使模型服务不可用，已有发布控制仍能按明确规则运行。
''','gitops canary trace','rollouts otel','两个 Agent 都说新版本正常，为什么仍不能自动视为充分证据？','它们可能共享同一错误假设或缺失数据。需要确定性的样本要求、时间窗口、指标规则、输出校验和测试；模型共识不能替代系统证据。',['self-healing-rollouts'],lab='12-rollouts')

write('24-capstone','交付一个可观测、可恢复的推理系统','把部署、缓存、通信、调度、观测与发布连接成一份可复查的工程结果。',r'''
最后一个项目不追求组件最多，而是让每个组件有明确职责、每个结论有证据。你将把同一套负载从单服务带到工作组，再加入观测与发布控制。

## 第一阶段：建立可信基线

固定 vLLM 镜像、模型 revision、输入输出长度和 GPU 配置。完成健康检查、流式客户端、负载生成、指标快照和冷/热启动记录。用相同负载重复测试，确认波动范围。

交付 `environment.json`、启动配置、原始 JSONL 测量记录和一页解释。报告 TTFT 或首内容口径，不混用；失败请求单独计数，不能静默丢弃。

## 第二阶段：扩展协作与资源管理

先验证多卡 AllReduce，再运行多卡模型；再进入多节点，验证 rank 发现、网络与全组预检。随后将工作负载纳入 Kubernetes/LWS，最后单独验证 DRA 的设备选择与节点准备。

每增加一层都保留上一个可运行版本。否则出现故障时，无法判断是模型、通信、容器、调度还是设备驱动引入的问题。

<Diagram id="capstone" caption="综合项目按可验证边界递进，每一阶段都留下配置、测量与恢复证据。" />

## 第三阶段：观察并恢复

接入请求、引擎、Pod、节点和 GPU 的关联信息，完成三个诊断场景：长输入、KV 压力、受控坏版本。Canary 在路由层切换稳定与候选服务，LWS 管理各自模型组，避免控制器所有权冲突。

回滚后确认新流量路由、在途请求和残留资源。Agent 仅在有足够证据时提出修复，修改通过评审与测试后进入下一次发布。

## 每项验收都对应可观察结果

| 验收项 | 需要提交的证据 |
|---|---|
| 服务能正确回答 | 请求参数、响应、模型与软件版本 |
| 性能结论成立 | 原始样本、负载分布、统计方法与比较条件 |
| 多卡通信正确 | rank 数、输入、预期与实际归约结果 |
| 调度与准备可解释 | Claim、Pod、驱动状态和事件 |
| 预检能阻止坏组 | 失败 rank、日志、退出码和组内结果 |
| 回滚有效 | 实际路由比例、分析状态、恢复后的指标 |

## 写一份真正有用的复盘

按照“我原本以为 → 测量看到了什么 → 为什么发生 → 我改了什么 → 还没有验证什么”组织。明确列出失败尝试，它们能帮助你理解边界。

不要把所有指标都写成改善。如果缩短首 token 的方案增加了显存或降低了吞吐，把代价写出来。工程判断的价值就在于知道为了哪个目标做了什么交换。

## 完成之后怎样继续深入

若通信成了瓶颈，进入 NCCL 同步与 KernelAgent；若多后端适配复杂，进入 PyTorch Generalization；若拓扑约束不清晰，进入 DRA 高级专题。上午 Keynote 则用于回顾生态位置，不需要记住所有项目名称。

你最终应能独立画出请求路径、解释主要状态、选择观测方法，并提出一个可验证的优化或恢复方案。这比“成功启动过所有组件”更接近掌握。
''','slo lws dra nccl canary','vllm lws dra rollouts','如果多节点推理变慢，你应从哪一个已建立的基线开始对比，避免随机改参数？','依次比较单服务、单机多卡、跨节点 collective、跨节点模型和 Kubernetes/LWS 路径的同负载记录，定位退化首次出现的边界，再检查对应通信、调度或资源条件。',lab='13-capstone')

write('01-kernelagent','KernelAgent：让优化服从真实硬件','从 Roofline、正确性和性能分布出发，读懂多轮 kernel 优化闭环。',r'''
KernelAgent 不是简单地“让模型写 CUDA”。它把编写候选、编译、运行、检查正确性、采集 profiler 和修订计划连接起来。真正可复用的资产是验证工具、硬件知识和失败记录。

## 先定义可以被反驳的计划

选一个小型 pointwise 或归约算子，固定 GPU、dtype、shape 和容差。估计最低读写字节数，再用有效带宽计算下界。如果最好的实测已经接近这个下界，继续追求大幅提升就需要解释如何减少字节流量，而不只是增大 block。

<Slide id="kernelagent-9" />

计划至少包含正确性参考、性能基线、测试矩阵、预期瓶颈和测量方法。输入 shape 改变时，计划也要改变。为一个固定矩阵调优的候选不一定适合小矩阵或不同 strides。

## 三层产物各自承担什么

Agent 基础设施提供工具、执行环境和上下文；方法层规定如何计划、检查和测量；产物层输出可运行实现、集成、测试和日志。缺少任一层，漂亮的代码片段都不足以形成可靠优化。

<Slide id="kernelagent-7" />

<Diagram id="agent-loop" caption="每轮候选先过正确性，再谈性能；失败也进入下一轮计划。" />

## Profiler 应该怎样影响下一轮

看到访存效率低，先检查布局与访问模式；看到寄存器压力大，检查每线程工作量；看到 launch 开销明显，考虑融合或图执行。不能因为 profiler 给出一个百分比，就断言某个优化必然有效。

每轮只改变能解释的因素，保留候选、编译参数、错误和测量分布。最优单次结果可能只是噪声，中位数和稳定性更能说明问题。

## 正确性测试必须覆盖边界

小尺寸、非整块大小、不同 strides、极端数值和不同 dtype 都可能暴露被 benchmark 隐藏的问题。归约顺序改变会影响浮点误差，容差必须服务于算法要求。

如果使用 `torch.custom_op` 集成，还要考虑设备、形状推断、autograd 和编译兼容性。集成成功与单独 kernel 运行成功是两项验收。

## 不同图表不能混成一个性能故事

<Slide id="kernelagent-15" /><Slide id="kernelagent-16" />

本地材料提到 2.02x、1.56x、65/100、89% roofline，以及某案例五轮约 40% 改善。这些对应不同任务、基线和硬件条件，不能写成“所有 kernel 平均提升 40%”。当前材料不足以复现完整研究结果，实验包只复现受控的优化纪律。

## 你的五轮实验

第一轮建立基线；第二轮验证主要流量；第三轮改变一种布局或执行参数；第四轮扩大正确性矩阵；第五轮重复测量并整合产物。若没有提升，写清楚哪些假设被否定，同样是有价值的结果。
''','roofline kernel tensor','compile cuda pytorch','某候选最快一次提升 20%，但中位数不变，能宣布优化成功吗？','不能仅凭最快一次。需要重复、预热、同环境测量，观察噪声与分布，并确保正确性与目标场景没有退化。',['kernelagent'],lab='03-kernel',kind='topics',prerequisites=['04-hardware','05-pytorch'])

write('02-generalization','PyTorch 通用化：统一接口与硬件差异','看懂框架后端抽象，而不把“写一次”误解为性能自动迁移。',r'''
把 `cuda` 字符串换成另一个设备名称，有时能让简单张量代码运行。但完整程序还涉及设备发现、Stream、Event、随机数、内存、图执行、通信和编译后端。通用化要处理的是一组契约。

## 用户入口与内部接口

`torch.accelerator` 提供面向加速器的公共入口；TorchInductor 内部的设备抽象则服务于编译器。它们处在不同层，不能把内部后端契约当作所有应用都该依赖的稳定 API。

<Slide id="pytorch-generalization-8" />

写应用时先依赖公开接口，再明确哪些部分是后端专用。例如通用张量运算可以复用，但 NVIDIA profiler 与某些 CUDA kernel 不能直接运行在其他硬件上。

## Stream 和 Event 为什么值得抽象

Stream 表达一条有序提交队列；Event 可以标记完成或建立依赖。统一名称帮助上层组织异步工作，但设备的同步语义、计时能力和支持范围仍需核对。

跨设备拷贝也不只是一个 API。是否异步、是否需要 pinned host memory、是否允许直接 P2P，都取决于路径与实现。接口看起来统一，不代表没有性能差异。

## 一个迁移审计方法

把代码中的设备行为分成三类：公开通用 API、公开后端专用 API、内部实现依赖。第一类优先复用；第二类需要替代与能力检测；第三类尽量移出应用边界，或者固定版本并接受维护责任。

```python
# 2.14.0 基线：先检查是否存在可用 accelerator。
import torch
available = torch.accelerator.is_available()
print('有可用加速器:', available)
if available:
    print('当前类型:', torch.accelerator.current_accelerator())
```

这只检查入口，不证明整个模型所有算子可运行。实际迁移还需要模型级正确性、编译和性能测试。

## 迁移的验收矩阵

逐项验证 dtype、算子覆盖、动态 shape、训练/推理、随机数、checkpoint、分布式通信和 profiler。对每项记录“支持、受限、不支持、未验证”，不要用一个总勾选掩盖局部差异。

先比较数值误差和任务质量，再比较性能。两个后端在同一容差内不意味着逐位相同，复现要求要与实际任务相符。

## 会议路线图怎样阅读

<Slide id="pytorch-generalization-15" />

会议展示的是当时的演进方向。Graph、memory、generator 等入口在不同版本的完成度可能不同，本文以当前稳定文档与 2.14.0 发布为核验基线。对未验证后端不作支持承诺。
''','tensor kernel gpu','accelerator pytorch compile','统一 accelerator API 是否意味着同一模型在不同硬件上性能一致？','不意味着。统一的是公共行为契约，实际算子实现、内存层次、通信和编译优化仍不同，需要分别验证正确性、覆盖范围和性能。',['pytorch-generalization'],kind='topics',prerequisites=['05-pytorch'])

write('03-optimizers','优化器为什么既要快，也要稳？','把横向融合、纵向融合、状态精度和优化算法分开讨论。',r'''
优化器负责根据梯度更新模型参数。即使矩阵计算很快，优化器对大量参数与状态的读取和写回仍可能占用可观时间。因此这个五分钟场次可以展开成一个很好的性能与数值专题。

## AdamW 保存了什么

除参数和梯度外，AdamW 维护一阶、二阶矩等历史状态。每次更新涉及缩放、加法、平方和归一化等操作。若每一步分别运行，可能产生很多 kernel launch 和中间显存读写。

<Slide id="speedy-optimizers-2" />

## foreach 与 fused 不是同义词

foreach 批量处理多个 tensor 上的同一种操作，减少逐 tensor 调用；fused 路径还能合并同一更新链中的多个步骤，减少中间结果写回。横向与纵向是理解两者的方向。

<Slide id="speedy-optimizers-3" /><Slide id="speedy-optimizers-5" />

支持条件受设备、dtype 和版本影响，不能只把 `fused=True` 加到所有优化器上。内存峰值、编译与图捕获兼容性也要测试。

## 状态精度下降能省多少

如果原先两个状态 tensor 使用 FP32，改为 BF16，其自身字节数约减半。但模型参数、梯度、激活和其他缓冲没有随之全部减半，所以总训练显存不会机械下降 50%。

<Slide id="speedy-optimizers-7" />

数值范围与有效精度是不同概念。BF16 保留较大的指数范围，但有效位较少；更新很小或长期累积时，误差可能影响收敛。只比较 step 时间不足以证明替换正确。

## Functional API 把责任交给谁

面向对象优化器通常管理参数组和状态。functional 接口显式接收参数、梯度和状态，更便于自定义，但状态创建、dtype、checkpoint 和参数对齐都需要调用方处理。

<Slide id="speedy-optimizers-11" />

内部 `_functional` 等路径不能未经说明就当稳定公共接口。实验优先比较公开 AdamW 的 single/foreach/fused 路径；讲义中的内部 API 用于理解设计，不直接做生产依赖。

## 一份有意义的比较

固定模型、数据批次、种子、学习率和训练步数；分别测 step 时间、峰值显存和损失曲线，再检查任务指标。每个配置初始化独立状态，避免把上一次训练结果带进下一组。

Muon 涉及矩阵更新的算法变化，不能与单纯融合混成一个开关。要比较它，需另设优化超参数与收敛评价，本教材把它列为延伸阅读。
''','tensor kernel bandwidth','optim pytorch','把优化器状态从 FP32 换成 BF16，为什么既不能保证总显存减半，也不能只测吞吐？','只有对应状态的存储项约减半，其他内存项仍在；精度变化可能影响累积误差与收敛，需要同时验证损失、任务指标和数值稳定性。',['speedy-optimizers'],lab='03-kernel',kind='topics',prerequisites=['04-hardware','05-pytorch'])

write('04-nccl-sync','NCCL 同步：谁什么时候可以读写？','用生产者、消费者和缓冲生命周期解释设备侧通信的正确性。',r'''
融合 kernel 最危险的错误往往不是“算错加法”，而是某个线程提前读了尚未可见的数据，或生产者重用了消费者仍在读取的缓冲。同步协议必须先于性能优化设计。

## 分清四个时刻

操作已发起、源数据可以重用、数据已到达远端、消费者可以读取，这些时刻并不总是同一个。具体接口只保证文档声明的完成层级，不能用名字猜测。

<Term id="gin" /> 的传输与 signal/counter 协议需要配合；<Term id="lsa" /> 的普通 load/store 也不能代替跨 rank 的可见性和阶段同步。

<Slide id="advanced-nccl-32" />

## 两 rank 归约的所有权

每个 rank 先写好本地输入，再共同确认输入可读；随后读取 peer 并计算输出；最后确认消费完成，才能进入可能覆盖输入的下一轮。若输入与输出原地重叠，更需要检查是否有 rank 提前覆盖其他 rank 尚未读取的数据。

<Slide id="advanced-nccl-21" />

课程中的练习图是协议框架，不是可直接复制的完整生产实现。具体 barrier、线程参与范围和内存 fence 要依据 2.31.2 API 与示例源码核对。

## 对称性是布局约定

<Slide id="advanced-nccl-27" />

window offset 对应同一逻辑数据位置，虚拟地址数值可能不同。每个 rank 注册大小或布局不一致，会使“相同偏移”指向不同含义的数据。创建/注册通常也是 collective setup，需要全体参与者遵守协议。

## 缓冲生命周期如何记录

为每个 buffer 写出分配、注册、生产、传输、消费、复用、注销、释放八个阶段，标明哪个 stream 和哪些 rank 使用它。复用与释放都应等待所有相关操作完成。

在单步正确后再循环多轮。很多竞态只在复用时出现，单轮结果正确不能证明长期安全。增加数据校验、不同消息大小和重复次数，记录首次不一致的 rank 与阶段。

## 性能测试不能隐藏正确性

先通过参考结果验证，再开启计时。遇到不稳定结果，不应只挑选“没出错的最快轮次”。使用 sanitizers 或调试工具时，也要说明工具覆盖范围与对时间的影响。

进阶实验从本地归档源码的固定 commit 出发，保留原历史，不自动复制整个嵌套仓库到公开站。实验包记录代码来源和硬件条件，未实际运行的部分明确标注。
''','nccl lsa gin rank','nccl nccl-api','为什么一次归约结果正确，不足以证明循环执行的融合 kernel 安全？','缓冲复用、跨轮同步和异步完成可能引入只在后续轮次出现的竞态。需要验证生产/消费/复用边界，覆盖多轮、不同大小与并发情况。',['advanced-nccl'],lab='07-nccl',kind='topics',prerequisites=['14-collectives','15-nccl'])

write('05-dra-topology','DRA 拓扑：让 GPU 与 NIC 的位置可比较','理解跨驱动属性、功能成熟度和控制面规模，而不把 roadmap 当配置。',r'''
“GPU 与 NIC 在同一个 NUMA”听起来简单，但两个驱动可能使用不同属性名称与表达方式。调度系统需要共同语义，才能比较来自不同资源提供方的信息。

## 拓扑为什么不能只靠设备数量

两套分配都包含一张 GPU 和一张 NIC，路径却可能不同。额外的 CPU 插槽或 PCIe 层级会影响延迟和带宽。更复杂的平台还有 NVLink fabric、分区和跨节点设备域。

<Slide id="kubernetes-dra-12" />

拓扑约束缩小候选集合，可能提高单任务效率，也可能降低可调度性。硬约束与偏好应根据真实需求选择，避免把所有“可能更快”的条件都写成必须满足。

## 属性必须具有可比较含义

一个驱动发布 `numa`，另一个叫 `numaNode`，名称与类型还可能不同。派生属性思路用表达式转成共同维度，再施加匹配约束。CEL（Common Expression Language）是用于表达选择/计算逻辑的语言，不是任意执行脚本。

<Slide id="kubernetes-dra-13" /><Slide id="kubernetes-dra-44" />

这些页涉及特定 KEP 与 feature gate。核心 DRA 可用，不代表所有高级功能都默认开启。实验应先读取目标版本功能表，再确认驱动确实发布了预期属性。

<Diagram id="dra-topology" caption="跨驱动约束需要共同语义，再由调度与节点准备落实；名称相似不是语义一致的证明。" />

## ComputeDomain 的生命周期

讲义讨论 NVIDIA 驱动管理或主机预置 IMEX 等方式。前者把更多生命周期绑定到工作负载，后者可能降低启动延迟但增加预置容量与清理责任。ComputeDomain 不是普通 GPU/NIC 共置的必需概念，而是特定平台进阶能力。

## 规模问题来自状态写入方式

许多 daemon 同时写同一对象可能制造冲突与热点。拆分状态对象、降低更新频率或让协调器汇总，都是控制面设计取舍。对象模型语义正确，不意味着大规模更新自动高效。

评估时记录设备数、ResourceSlice 数、更新频率、API Server 压力、冲突重试和启动延迟。不能只展示一份成功调度的 YAML 就宣称“支持大规模”。

## 版本表怎样维护

把已发布稳定能力、默认启用的 Beta、默认关闭的 Alpha、未合入提案分开。对每个实验记录 Kubernetes、driver、chart、feature gates 和实际 API schema。1.38 计划保留为会议背景，不加入 1.37 的可执行路径。
''','dra claim numa','dra dra-features','开启核心 DRA 后，为什么不能假定讲义中的所有高级拓扑 YAML 都能运行？','高级功能有独立版本、成熟度与开关，还依赖驱动属性和准备能力。必须逐项核对 API schema、feature gate 与驱动支持。',['kubernetes-dra'],lab='09-dra',kind='topics',prerequisites=['16-gpu-scheduling','17-dra'])

write('06-ecosystem','大会全景：从机器到有效 token','把上午 Keynote 中的趋势放回具体系统问题，不把宣传数字当学习结论。',r'''
两天上午包含多个短演讲，主题从开源生态、异构硬件到 Agent 运行时。它们适合帮助你判断“这件事发生在哪一层”，但不足以独立证明某个系统的实现与性能。

## Token factory 改变了什么问题

机器数量和 GPU 忙碌率是资源视角，有效 token 和用户服务目标是产出视角。前者容易计数，后者更接近业务价值，但必须定义输出质量、延迟、成本和失败率。一个无效回答产生再多 token，也不代表有效产出更多。

## 异构硬件需要抽象边界

GPU、NPU、XPU 等加速器并存，框架希望上层表达共同行为，底层保留优化能力。PyTorch Generalization 和 DRA 分别在框架与资源控制层处理这种问题。它们是不同层的抽象，不能互相替代。

## Agent 拉长了生命周期

一次 Agent 任务可能包含多轮模型调用、工具访问和状态变化。缓存、权限、日志和恢复都不能只跟单个 HTTP 请求绑定。进程结束不等于外部影响消失，会话结束也需要释放不再使用的资源。

<Slide id="2026-09-08-keynotes-8" />

## 看短演讲时留下三个问题

它要解决哪个具体痛点？解决方案依赖什么条件？有什么公开文档、代码或实验能支持？把尚无证据的性能数字留在“讲者报告”中，不强行编成通用知识。

两条 Keynote 录音都存在边界限制：开始时间与官方开场不完全重合，第二天录音没有覆盖全部后段。资料库保留覆盖说明，不根据缺失录音补写不存在的现场内容。
''','gpu slo kv-cache controller','schedule accelerator dra', '为什么“GPU 利用率更高”不自动等于“有效 token 成本更低”？','有效产出还依赖回答质量、延迟、错误、缓存与资源成本。更忙可能来自无效工作、通信或等待，需要端到端口径。',['2026-09-08-keynotes','2026-09-09-keynotes'],kind='topics',prerequisites=['01-request'])

write('07-openinfra','OpenStack 与 Kubernetes：迁移到底在迁什么？','从虚拟机、容器和团队能力理解开放基础设施迁移。',r'''
OpenStack 和 Kubernetes 经常一起出现，但不是同一个东西。OpenStack 提供计算、网络、存储等云基础设施服务；Kubernetes 主要编排容器化工作负载。Kubernetes 集群可以运行在虚拟机上，两者可以分层协作。

## 先分清资源层与应用层

虚拟机具有自己的 guest 操作系统内核；容器通常共享宿主内核。迁移虚拟机涉及镜像、磁盘、网络和运行平台；把应用改造成容器还涉及配置、状态、部署和运行方式。把虚拟机搬到开源平台，不等于已经完成应用现代化。

## Vendor lock-in 不只是许可证

如果团队无法解释、升级、恢复或替换一个关键组件，即使软件开源，也可能仍依赖单一服务商。开放接口、可移植数据、文档、人才和恢复演练一起决定实际控制能力。

## 会议中的迁移方法

Viettel 案例强调 pilot、迁移工厂、跨职能团队和社区参与。pilot 是先选择少量可控工作负载验证；迁移工厂则把重复评估、迁移和验收步骤标准化。

现有妙记没有配套公开讲义，部分数字与专名仍有识别歧义。本站保留方法论，不把缺乏分母的百分比写成普遍成功率。

## 一个可执行的迁移评估表

按业务重要性、状态性、依赖、数据量、停机容忍、恢复要求和团队所有权分组。先选回滚路径清楚的 pilot，验证网络、存储和监控，再扩大范围。

迁移验收不能只有“能开机”：还要检查业务正确性、备份恢复、性能、权限、运维交接和退出旧平台的条件。明确哪些应用只搬运行平台，哪些需要重新设计。

## 与 AI Infra 主线的联系

模型平台同样有权重与数据、网络、加速器和工作负载编排。能否开放替换某一层，依赖上层是否通过明确契约使用它。前面的 PyTorch、NCCL、DRA 讨论，最终都回到这条工程原则。
''','pod controller gpu','openstack schedule','把虚拟机迁到 OpenStack，是否意味着应用已经完成容器化？','不是。运行平台迁移与应用架构改造是不同工作，容器化还涉及配置、状态、生命周期与部署方式，并需要独立验收。',['openinfra-migration'],kind='topics',prerequisites=['02-kubernetes'])

write('01-serving','实验 01 · 固定版本并运行 vLLM','准备镜像与模型 commit，验证健康、接口与资源信息。',r'''
本实验需要可运行 vLLM 0.29.0 的 NVIDIA GPU、兼容驱动、Docker 与 NVIDIA Container Toolkit。它尚未在作者本机执行；命令和版本已作静态核对，硬件结果需要你实际记录。

## 准备与固定输入

下载实验包，进入 `labs/serving`。运行 `python3 prepare.py`，它通过模型仓库 API 解析当前 commit，写出 `experiment.env` 与 `environment.json`。这一步只解析元数据，不运行 GPU。

```bash
cd labs/serving
python3 prepare.py
bash serve.sh
```

`serve.sh` 固定镜像标签并保存拉取到的 digest，使用解析后的模型 revision；缓存目录绑定到当前实验目录。请同时保存 `nvidia-smi` 输出和启动日志。模型 commit 锁定后，即使默认分支更新，本次实验仍引用相同权重。

## 验证服务边界

在另一终端请求 `/health`、`/v1/models` 和 `/metrics`。确认 served model name 为 `lab-model`，再运行一次流式请求。成功 HTTP 状态不是唯一验收，还要保存非空输出及最终使用量。

```bash
curl -f http://127.0.0.1:8000/health
curl -f http://127.0.0.1:8000/v1/models
curl -f http://127.0.0.1:8000/metrics > metrics-before.txt
python3 measure_stream.py --requests 1 --output smoke.jsonl
```

## 如何解释失败

设备不可见时先查容器 runtime 与驱动；加载 OOM 时查模型、上下文预算与其他进程；模型名错误时查接口返回。不要把所有错误都归为“vLLM 不稳定”。

## 验收与清理

提交环境文件、镜像 digest、启动日志、smoke JSONL 与指标快照。记录冷启动时间，随后热身一次再进入性能实验。结束后运行 `docker stop inference-lab`；容器会删除，模型缓存与结果文件保留。不要删除结果后才写复盘。
''','gpu kv-cache','vllm vllm-serving','为什么固定镜像标签之后还保存 digest？','标签是人类可读版本标识，digest 对应具体镜像内容，能提高复查时确定性。模型 revision 和硬件环境仍需分别记录。',kind='labs',prerequisites=['06-vllm'])

write('02-attention','实验 02 · 验证 K/V 复用','在 CPU 上运行一个可手算的注意力例子，确认缓存结果与完整计算一致。',r'''
本实验只需要 Python 3 标准库，没有 GPU 依赖。它验证注意力中间结果复用的数学结构，不模拟完整语言模型的预测能力。

## 运行前先预测

历史向量为 `[1,0]` 和 `[0,1]`，新位置为 `[1,1]`，演示中 Q/K/V 变换都取单位矩阵。先计算新查询与三个键的缩放点积，再估计 softmax 权重。

```bash
python3 labs/attention/attention.py
```

脚本比较完整重建历史与复用历史缓存的输出，要求每一维误差小于 `1e-12`。输出包含两条向量与 `equal: true`。这里的阈值适用于这个小型双精度示例，不能照搬到 BF16 GPU kernel。

## 改变一个条件

把一个历史向量改掉但保持旧缓存不变，预测结果是否仍一致。再同步更新缓存，重新比较。这个实验说明缓存有效性依赖输入与计算条件，不能随意跨上下文复用。

增加第三个历史位置，观察缓存元素数量如何增长。记录查询维度、历史长度、K/V 元素数，连接到主线的容量公式。

## 验收与边界

提交手算权重、脚本输出和修改历史后的差异解释。该脚本没有多层、位置编码、训练或采样，不能由它推断真实模型速度。没有外部资源需要清理；保留你的修改副本和结果。
''','attention tensor kv-cache','attention','为什么这个实验通过不能证明某个真实引擎的 prefix cache 实现没有 bug？','它只验证简化数学结构，不覆盖多层、位置编码、分页、并行布局、并发和实际实现。真实引擎还需要对应的集成与边界测试。',kind='labs',prerequisites=['03-attention'])

write('03-kernel','实验 03 · 正确性先于 GPU 性能','比较 eager 与编译执行，再对照公开 AdamW 路径。',r'''
需要 CUDA GPU 与 PyTorch 2.14.0。选择与驱动兼容的官方 wheel，记录实际 CUDA/NCCL 版本。实验脚本在无 GPU 时明确失败，不生成替代性能数据。

## 先跑正确性，再计时

```bash
python3 labs/kernel/benchmark.py > kernel-results.json
```

脚本对相同输入检查 eager 与 compiled 结果，再分别热身，使用 CUDA events 记录 30 组样本，每组重复 100 次。JSON 保留中位数与全部样本，而不是只给最快值。

先预测融合是否减少 launch 和中间写回，再观察 profiler。若输入很小，固定开销可能占主导；改成不同 shape 后，编译和资源占用也会变化。每次修改建立独立结果文件。

## 观察优化器的另一种融合

```bash
python3 labs/kernel/optimizers.py > optimizer-results.json
```

single、foreach 和 fused 使用相同种子创建新的参数与状态，避免前一组训练影响后一组。记录平均 step 时间、峰值分配与损失样本。这个二次函数优化只是路径对照，不等于真实模型收敛评估。

## 边界测试

增加非整块尺寸、较小张量和不同 dtype。先检查结果，再接受性能。不要为了让测试通过任意放宽容差。把编译首次调用和稳定运行分开，记录是否发生重新编译。

## 验收与清理

提交脚本配置、环境、原始样本、正确性结果及一次无收益或失败的尝试。脚本退出后释放 GPU 进程；确认没有遗留进程占用设备。设备端实验在作者环境未运行，网站不展示虚构提升数字。
''','kernel roofline tensor','pytorch compile optim','为什么优化器比较要为每条路径重新初始化参数和状态？','否则不同路径会从不同训练阶段开始，损失与更新条件不一致，性能和数值比较都缺少公平基线。',kind='labs',prerequisites=['04-hardware','05-pytorch'])

write('04-latency','实验 04 · 测量首内容、并发和长度','使用原始 JSONL 记录，分离三个变量对用户等待的影响。',r'''
先完成实验 01，保持服务镜像、模型 revision 和参数不变。该客户端使用 Python 标准库；真实延迟需要连接运行中的服务。

## 三组实验，一次改变一个变量

```bash
cd labs/serving
python3 measure_stream.py --requests 5 --output warmup.jsonl
python3 measure_stream.py --requests 100 --concurrency 1 --output base.jsonl
python3 measure_stream.py --requests 100 --concurrency 4 --output concurrent.jsonl
python3 measure_stream.py --requests 100 --repeat-input 50 --output long-input.jsonl
python3 measure_stream.py --requests 100 --max-tokens 256 --output long-output.jsonl
```

`repeat-input` 增加文本重复次数，不保证准确 token 数；使用引擎 usage 记录实际输入量。`max-tokens` 是输出上限，不保证模型一定生成到上限。比较时报告实际长度分布。

## 脚本测到什么

请求起点使用单调时钟；忽略空内容事件，记录首次非空文本到达、各 chunk 与完成时间。chunk 可包含多个 token，因此输出指标叫 first-content，不冒充精确 ITL。脚本保留失败请求与错误信息。

这是固定并发的闭环压测，响应变慢会影响后续请求提交节奏，不等同固定到达率的开放负载。接近容量上限时，还应另做开放负载测试。

## 配合引擎证据

每组前后保存 `/metrics`，从实际 HELP/TYPE 找到延迟、队列、缓存和请求相关指标。p95 用 nearest-rank 计算，100 个样本仍有波动；关键结论应增加样本并重复。

## 验收与清理

提交五组 JSONL、错误率、实际输入输出分布、中位数/p95 和一段因果解释。若只有客户端变慢，检查网络和缓冲；若引擎排队增加，继续研究并发预算。实验后停止压测即可，服务可供后续实验复用。
''','ttft itl tpot throughput','vllm-metrics prometheus','max-tokens=256 是否意味着每个请求都有 256 个输出 token？','不是。它是上限，模型可能提前结束。必须记录真实 usage，避免把不同输出长度的请求直接比较。',kind='labs',prerequisites=['07-latency','08-batching'])

write('05-cache','实验 05 · 缓存命中与实际收益','对照相同前缀、不同前缀和冷/热状态，解释缓存优化是否成立。',r'''
使用实验 01 的固定模型与 vLLM 版本。缓存实验应独占或明确控制其他负载，否则缓存状态会被其他请求改变。

## 建立对照组

准备长度相近的三组输入：完全相同前缀、相同长度不同内容、从前缀中间开始修改。保持输出上限与实际输出分布可比较。先通过模型 tokenizer 或 usage 记录 token 长度，不用字符数替代。

分别用 vLLM 0.29.0 支持的 prefix caching 开关启动独立服务，先运行 `--help` 核对该版本的启用/禁用参数。重启服务可以建立冷缓存组，但也会带来编译与加载影响，因此先完成与测试内容无关的热身。

## 记录两种命中和最终目标

收集请求级复用、token/字节复用、TTFT/first-content、KV 占用与抢占。先从真实 `/metrics` HELP 查找该版本字段，再保存查询。指标可能是累计 Counter，比较时取测试窗口增量。

用第 11 节缓存演示改变有效带宽，理解为什么命中不保证收益。网站策略模拟仅说明淘汰机制，不作为 UCM 实验复现。

## 访问 trace 怎么保存

每条记录包含 session、时间、块的前缀关系、大小、访问/结束事件及加载/重算测量。不得凭观察到的文本相同就自行给不同模型版本共享 KV。重算成本需要相同负载下的实际对照。

## 验收与故障场景

验证前缀中段修改不会错误复用后续状态；缓存容量不足时记录淘汰或抢占；远端缓存故障时验证预算与重算/失败路径。若没有部署远端 connector，就明确标为未测试。

提交原始请求、指标增量、延迟分布与可复用条件。清理服务与缓存状态前保存结果；不要删除模型权重作为清缓存的替代操作。
''','kv-cache prefill ttft','vllm-prefix vllm-metrics','为什么重启后的第一次请求不能直接与热缓存请求比较后归因于 APC？','第一次可能包含加载、编译和其他热身。需要分离冷启动、计算热身与缓存状态，使用控制输入建立公平对照。',kind='labs',prerequisites=['09-kv-memory','10-prefix','11-tiered-cache'])

write('06-distributed','实验 06 · 从单机多卡到多节点与 PD','先验证通信，再增加并行与传输边界。',r'''
需要多张 NVIDIA GPU，跨节点部分还需要可达的网络与一致运行环境。先完成实验 07 的通信正确性，避免直接用完整模型调试网络。

## 单机 TP 基线

在相同固定镜像中，为支持的模型增加 `--tensor-parallel-size 2`，保持模型 revision、长度与负载。检查模型头数等条件是否兼容切分。记录两张 GPU 的显存、吞吐与延迟，不预设提升比例。

## 两节点 MP 配置

vLLM 当前文档提供多节点 multiprocessing 入口。以下是每节点一 GPU、TP=1、PP=2 的命令形状；模型必须支持相应 PP，所有节点使用同一模型 revision 和版本。

```bash
# 节点 0；MASTER_ADDR 设置为实际可达地址
vllm serve Qwen/Qwen2.5-0.5B-Instruct --revision "$MODEL_REVISION" \
 --tensor-parallel-size 1 --pipeline-parallel-size 2 \
 --nnodes 2 --node-rank 0 --master-addr "$MASTER_ADDR" \
 --served-model-name lab-model --max-model-len 4096
# 节点 1：相同参数，但 node-rank=1 并增加 --headless
```

正式执行前用锁定镜像 `vllm serve --help` 核对参数。保存每个 rank 的日志与网络路径；不要把 host IP、容器 IP 和 GPU 编号混用。

## PD 作为独立对照实验

PD 分离不等于 PP。前者切分输入与输出阶段，后者切分模型层。使用 vLLM 0.29.0 仓库同 tag 的 connector 示例，先读兼容矩阵，再选择与硬件一致的路径。

记录模型、KV dtype、布局、TP/PP、connector 版本及传输字节。验证传输失败、取消与超时清理；若没有完成这些，不标为可可靠运行的 PD 系统。

## 验收与清理

提交单卡、单机多卡、跨节点三组原始结果，以及额外 PD 对照（实际运行时）。比较相同长度分布与并发，不把不同 GPU 数下的总吞吐误写成单请求速度。

停止所有 rank 的服务与相关运行时，检查节点 GPU 进程、监听端口和缓存分配均已回收。硬件实验未在作者本机执行。
''','tp pp rank kv-cache','vllm-distributed vllm-pd','PP=2 和 Prefill/Decode 分离是一回事吗？','不是。PP 沿模型层切分阶段；PD 将输入处理与后续生成分到不同实例，需要额外 KV 交接。',kind='labs',prerequisites=['12-disaggregation','13-parallelism','14-collectives'])

write('07-nccl','实验 07 · 两 rank 通信与同步证据','从有确定答案的 AllReduce 开始，再进入 Device API。',r'''
基础实验需要两张 CUDA GPU 与 PyTorch 2.14.0；它使用 PyTorch 安装包实际链接的 NCCL，脚本打印版本。Device API 是另一个固定 NCCL 2.31.2 的进阶环境，不假定两者版本相同。

## 先手算预期结果

rank 0 输入 `[1,2]`，rank 1 输入 `[2,4]`，AllReduce SUM 后每卡应得到 `[3,6]`。脚本使用 90 秒初始化超时并在结束时销毁通信组。

```bash
torchrun --standalone --nproc-per-node=2 labs/nccl/allreduce.py
```

若失败，先检查设备暴露与 rank 分配，保存所有进程日志。单个 rank 的成功输出不等于整个组通过。

## 跨节点之前检查什么

确保相同 Python、PyTorch、驱动兼容条件与脚本；确认 rendezvous 地址与端口可达；再用明确 node-rank 与节点数量启动。主线预检先检查本地条件，再检查全组通信。

## Device API 进阶路径

使用本地归档 `code/advanced-nccl/` 的固定 commit，先阅读 lsa-simple 和 barrier 的 README。记录 NCCL 2.31.2、CUDA 编译器、GPU、P2P 能力与 GIN 后端条件。不要将不具备硬件能力的失败当成程序逻辑错误。

为每个 window 写出注册、写入、可见、消费、复用与释放时序。至少覆盖多轮和不同大小；只有 correctness 通过后才能加入计时。

## 验收与清理

提交 rank/world、实际值、预期值、运行时版本和完整退出状态。带宽实验额外区分算法带宽与总线带宽。终止异常运行后检查所有 rank 已退出，避免孤立进程继续占用 GPU。

本站验证过小数组数学预期与脚本语法，但未在本机运行真实 NCCL；不会提供虚构带宽结果。
''','rank allreduce nccl lsa gin','nccl nccl-api','为什么基础 AllReduce 脚本打印的 NCCL 版本可能不是 Device API 实验固定的版本？','基础脚本使用 PyTorch 安装包实际链接的 NCCL；独立 CUDA Device API 示例使用另一套显式编译/运行依赖，必须分别记录。',kind='labs',prerequisites=['14-collectives','15-nccl'])

write('08-kubernetes','实验 08 · 在 Kubernetes 上运行推理服务','先建立传统设备资源路径，再将工作组与 DRA 作为独立扩展。',r'''
需要 Kubernetes 1.37 测试集群、GPU 节点、兼容容器 runtime 与已安装 NVIDIA Device Plugin。下载包中的 YAML 使用真实扩展资源名 `nvidia.com/gpu`，但你的集群必须确实提供它。

## 提交前先看资源

```bash
kubectl get nodes -o wide
kubectl describe nodes
kubectl apply --dry-run=server -f labs/kubernetes/vllm.yaml
```

确认 GPU 资源与可调度节点。为正式实验把模型 `--revision` 参数添加为实验 01 解析的 commit；原 YAML 展示部署结构，不应让变化中的模型默认分支破坏比较。

## 部署与观察

```bash
kubectl apply -f labs/kubernetes/vllm.yaml
kubectl get pods -n inference-lab -w
kubectl logs -n inference-lab deployment/vllm
kubectl port-forward -n inference-lab service/vllm 8000:8000
```

在另一终端运行实验 04 客户端。分别记录 Pending、容器创建、模型加载、startupProbe 与 Ready 的时间。Service 使用 ClusterIP，port-forward 仅提供本机访问。

## 故障不要只靠重建解决

Pending 查调度事件；容器启动失败查镜像、资源和 runtime；加载 OOM 查预算；Ready 失败查健康接口和日志。保存事件后再调整配置，每次只改一个变量。

## 验收与清理

提交声明、实际 Pod/节点映射、模型 revision、日志和流式请求结果。结束时先停止 port-forward，再删除本实验 Deployment 和 Service。若删除整个 `inference-lab` namespace，要确认其中其他实验结果已经保存，因为会连同其他对象一起删除。
''','pod gpu controller','k8s device-plugin vllm','为什么 server-side dry-run 通过，仍不能宣布推理部署成功？','它验证 API 接受与部分准入规则，不执行实际调度、设备准备、镜像启动、模型加载和请求处理。',kind='labs',prerequisites=['16-gpu-scheduling'])

write('09-dra','实验 09 · 观察 Claim 分配与设备准备','使用实际安装的驱动类别，跟踪 DRA 的不同阶段。',r'''
需要 Kubernetes 1.37 与支持相应设备的 DRA 驱动。驱动安装涉及平台条件，先选择并固定适用于目标硬件的官方发行版；本教材不把某厂商的全部高级功能当作核心 DRA 默认能力。

## 查真实库存与类别

```bash
kubectl get deviceclasses
kubectl get resourceslices
kubectl explain resourceclaim.spec.devices.requests --api-version=resource.k8s.io/v1
```

下载包的 `dra.yaml` 中 `example-gpu-class` 必须替换为实际类别。检查 ResourceSlice 的设备属性与可达节点，并记录驱动版本、chart 和 feature gates。

## 分阶段提交与观察

```bash
kubectl apply --dry-run=server -f labs/kubernetes/dra.yaml
kubectl apply -f labs/kubernetes/dra.yaml
kubectl get resourceclaims -n inference-lab -o yaml
kubectl describe pod dra-consumer -n inference-lab
kubectl logs dra-consumer -n inference-lab
```

这个消费者只运行 `nvidia-smi`，验证设备暴露，不运行模型。`ResourceClaim` 的分配结果、Pod 绑定和设备准备是独立检查点。

## 加入一个失败对照

使用测试专用声明请求不存在的 DeviceClass 或不满足条件的设备，预测失败会停在哪个阶段。不要更改共享集群的真实设备健康状态。保存事件和结果后删除失败对照。

高级 NUMA 匹配单独进行，只有目标版本和驱动属性均支持时再加入。会议中的 1.38 计划字段不进入本实验可执行路径。

## 验收与回收

提交真实类别、库存片段、Claim 分配、Pod 事件与设备可见输出。删除 Pod，再删除 Claim，观察驱动解除准备和资源回收。未验证回收就不能标记生命周期实验完成。
''','dra claim numa','dra dra-features','应该先删除消费者还是直接假设 Claim 删除就回收设备？','按资源使用关系处理：结束消费者并观察节点解除准备，再删除不再需要的 Claim，确认状态与资源回收。具体保护行为依赖 API 与驱动实现。',kind='labs',prerequisites=['17-dra'])

write('10-preflight','实验 10 · 让失败发生在模型加载前','验证本地检查、全组检查与有限失败处理，不把局部通过当整体通过。',r'''
需要完成多节点通信基线以及 LWS 0.10.0 安装。先读取已安装 CRD，确认实际字段；会议提案里的重启预算字段不能未经核对直接应用。

## 本地检查只是第一层

```bash
bash labs/kubernetes/preflight.sh
```

脚本验证设备、PyTorch runtime 和可选对端 DNS，并打印结构化结果。它明确不提供全组成功结论。挂载、模型 revision、网络接口和设备拓扑可按环境增加检查，但每项都必须有失败退出码。

## 再执行全组 collective

把实验 07 的 AllReduce 放到所有 rank 的预检阶段，固定 rendezvous 与超时。任意成员失败时，全组不得进入模型服务阶段。保存每个 rank 的开始、结束、节点和检查结果。

在 LWS 中集成前，先验证 init-phase peer discovery 是否适用于当前 startup policy，避免 leader 等 worker、worker 又等 leader Ready 的循环。参考同版本官方示例，不只复制一段 init container。

## 两个受控失败场景

为实验实例提供错误的对端地址，确认等待有超时；再让一个 rank 的检查显式失败，确认其他成员不会部分进入 serving。不要用无限重启来“修复”固定错误配置。

预算和组恢复由实际控制器/外层实验编排负责。若当前版本不提供计划中的字段，用明确的外部停止条件记录实验边界，不伪造 API 支持。

## 验收与清理

提交正常通过、DNS 超时、单 rank 失败三组日志和状态。至少证明模型加载在失败情况下没有继续。保存失败 Pod 证据后再清理工作组、预检资源和残留进程。
''','lws rank pod nccl','lws lws-api lws-preflight','为什么 leader 等所有 worker 完成 init，同时 worker 等 leader Ready，可能死锁？','两个条件相互依赖，任何一方都无法先满足。必须设计 init 阶段可用的发现方式和启动顺序，不能只套用服务 Ready 条件。',kind='labs',prerequisites=['18-lws'])

write('11-observability','实验 11 · 用指标和时间线验证故障','建立采集基线，再调查长输入、并发和缓存压力。',r'''
需要运行中的 vLLM 服务。基础指标采集使用 Prometheus 3.5.0，固定版本用于实验；请求级 trace 和 GPU collector 作为明确的后续集成，不伪造不存在的 span。

## 启动最小采集器

```bash
cd labs/observability
docker compose up -d
```

访问本机 `http://127.0.0.1:9090`，在 Targets 检查 `vllm-lab`。配置通过 `host.docker.internal:8000` 访问宿主服务；Linux 映射已写入 compose。服务在另一机器时修改为实际可达地址并记录。

## 从真实指标建立查询

先访问服务 `/metrics`，读取 HELP/TYPE。Counter 看窗口增量/速率，Gauge 看当前值，Histogram 先确认桶与单位。不要从旧文章复制字段后把空图解释成零。

运行实验 04 三组负载，保存请求 JSONL、Prometheus 查询与时间窗。记录队列、输入输出、KV 使用和抢占，判断变化是否支持你的假设。

## 加入追踪与设备关联

按照锁定版本的引擎 OTel 示例接入 collector 与 trace backend。验证真实请求 context 传播，记录 Pod UID、节点与 GPU UUID 映射。若引擎没有某一阶段 span，就保留缺失，不手工补成仿真证据。

GPU 指标采集需要对应平台 exporter 与权限。验证抓取间隔、时间同步和设备标识，再将同窗口数据与请求阶段关联。共享 GPU 的指标不作精确单请求归因。

## 验收与停止

完成长输入、并发增加和 KV 压力三份调查记录，每份包括假设、证据、反证与动作。保存 dashboard/查询导出、原始样本和未验证项。最后 `docker compose down`，确认采集器停止；模型服务按其他实验需要保留。
''','trace ttft kv-cache slo','otel otel-genai prometheus vllm-metrics','指标面板没有数据，能否当作服务没有请求或没有错误？','不能。先检查 target、抓取错误、字段名、标签、时间窗与采样。观测链路失败与业务值为零必须区分。',kind='labs',prerequisites=['19-signals','20-correlation','21-diagnosis'])

write('12-rollouts','实验 12 · 验证回滚与分析边界','用可控错误服务检查发布控制，再应用到推理服务路由。',r'''
需要测试 Kubernetes 集群和 Argo Rollouts 1.10.0。第一阶段使用实验包的 CPU HTTP 服务，只验证发布控制，不模拟模型性能。

## 准备可控坏版本

`labs/rollouts/app.py` 的 `/health` 返回健康；普通请求在 `FAIL=true` 时返回 500，或由 `LATENCY_SECONDS` 注入延迟。这个设计能证明“健康接口通过”不代表业务路径正常。

```bash
docker build -t inference-rollout-fixture:lab1 labs/rollouts
```

将镜像导入测试集群或推送到你有权限的 registry，记录 digest。不要把本机镜像标签当作所有节点都能拉取的地址。

## 建立稳定与候选服务

使用 Argo 官方同版本 Canary 示例，将容器替换为该 fixture。先验证基础 Rollout 的暂停、继续和终止；再增加支持的路由集成。没有路由权重控制时，只把副本比例作为近似实验，不能声称精确流量百分比。

把业务错误率和足够样本要求写入 AnalysisTemplate。无数据、查询失败或样本不足必须进入明确的失败/不确定路径。

## 四个故障检查

候选版本返回错误；监控后端不可用；stable/canary 窗口错开；Agent 结果结构不合法。每项记录实际 AnalysisRun 状态，验证不会因空结果自动继续。

Agent 适配器接收有限决策结构，修复只输出分支与 PR 草稿，经过测试与评审才进入下一次发布。两个 Agent 的共识不替代门禁条件。

## 应用到推理工作组

稳定和候选各有独立模型服务/LWS 组，在路由层切换新流量。不要让 Rollouts 和 LWS 同时控制同一份 Pod 模板期望。记录在途流式请求的 drain 行为。

## 验收与清理

提交实际流量比例、分析结果、错误率、回滚后请求和 Agent 负例结果。保存所有事件后删除实验 Rollout、分析对象和 fixture 服务；保留 controller 是否删除按测试集群共享范围决定。
''','canary gitops slo','rollouts rollouts-release','fixture 健康接口正常但业务接口 500，验证了什么？','验证探针通过不代表业务成功，发布分析必须观察真实业务路径与样本，不能只依赖 Pod Ready。',kind='labs',prerequisites=['22-canary','23-agent-repair'])

write('13-capstone','实验 13 · 综合交付与复盘','串联前十二个实验，形成自己的系统说明、测量证据和恢复手册。',r'''
使用独立测试环境，先建立结果目录并记录所有版本。这个实验没有“一键成功”的快捷入口，因为重要的学习目标是能解释每个边界。

## 三个阶段的进入条件

第一阶段完成实验 01、02、04，确认请求、数学与测量口径。第二阶段完成 07，再推进 06、08、09、10，确认通信、调度、准备和组启动。第三阶段完成 11、12，确认观测和回滚。

03 与 05 用于解释性能，不能跳过正确性就把它们的优化加入最终版本。

## 建议的结果目录

```text
results/
  environment.json
  configs/
  raw-requests/
  metrics/
  traces/
  events/
  failures/
  report.md
```

每次实验保存不可混淆的运行 ID、开始结束时间、版本、负载及结果。原始请求中不要包含真实私人或业务敏感文本。

## 完整验收

用同一负载比较单卡、多卡与跨节点；验证设备选择和准备；证明一个失败 rank 阻止全组进入服务；证明坏版本停止接收新流量且稳定路径恢复；解释未测量的 GPU 请求归因边界。

性能报告至少包含错误率、输入输出分布、首内容或 TTFT 口径、吞吐、KV 与硬件条件。只有实际执行的实验才勾选完成，未验证项写进结论。

## 最后一页复盘

回答五个问题：我原先怎样理解系统？哪个实验推翻了它？哪条证据最关键？改动换来了什么代价？下次遇到相同症状会先查哪里？

## 清理与恢复

先导出事件和原始样本，再按应用、工作组、Claim、采集器的关系逐步清理。检查 GPU 进程、容器、端口与存储残留。恢复测试前修改过的配置，并保存最终资源清单。
''','slo trace dra lws canary','vllm dra rollouts','综合项目完成的标准是启动了所有组件吗？','不是。需要可复查的正确性、测量、故障与恢复证据，并能解释各组件职责、取舍和未验证边界。',kind='labs',prerequisites=['24-capstone'])
