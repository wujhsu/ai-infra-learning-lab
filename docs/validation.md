# 验证记录

本地验证日期：2026-09-13。

## 已运行

- Astro 类型检查：0 errors / 0 warnings。
- 8 项 TypeScript 测试：请求时间、KV 容量、连续批处理、缓存收益与淘汰、通信计算重叠、DRA 筛选、导入字段验证、冲突合并。
- 6 项 Python 测试：SSE 空角色事件与真实文本、HTTP 失败、流中断、Agent 决策拒绝/接受，以及健康路径 200 与业务路径 500 的真实 HTTP fixture。
- CPU Attention 小实验：缓存路径与完整重算结果一致。
- 静态构建：66 页 HTML，24 主线 / 7 专题 / 13 实验；Pagefind 建立 59 个可搜索页面的索引。
- 内容与链接验证：214 页讲义索引，53 组标准/高清图，1,540 个本地链接或资源引用通过；发布 HTML 不含原始妙记路径或个人身份字段。
- 浏览器操作：六个演示展开；KV 8 GiB、缓存加载 520 ms、流水线 23 ms、TTFT 1450 ms / 总时间 2690 ms 与计算测试一致。
- 浏览器操作：术语解释、基础补课侧栏、讲义区域高亮、高清图片打开与关闭、键盘 Enter / Space / Escape。
- 本地记录：笔记和「能解释」状态刷新后保留；JSON 下载、文件导入预览、重复记录处理、导入前备份下载、Markdown 导出。
- 设置：深浅主题、20 px 字号、2.1 行距刷新保留，且不覆盖笔记。
- 搜索：「首字延迟」和 ResourceClaim 均返回结果，子结果可定位正文段落。
- 390 px 手机、820 px 平板、1440 px 桌面布局无页面横向溢出；浏览器打印渲染图检查了首页、正文和深色平板布局。Ego 的截图接口超时，版面检查使用同一浏览器 Page.printToPDF 渲染，不宣称完成真机截图测试。
- 禁用 JavaScript 后：样板正文、9 个二级标题、7 个数学节点及术语静态解释仍可读取。

## 没有在本机执行

GPU / CUDA 性能基准、真实 vLLM 模型服务、跨节点 NCCL、Kubernetes / DRA 驱动准备、LWS 组恢复、Argo Rollouts 控制器行为、真实 OTel 和 GPU exporter 组合。示例通过版本代码/字段核对和本地可运行部分验证，真实实验需按教材检查点记录结果。

新增硬件结果应包含环境、镜像 digest、模型 revision、命令、预热、负载、样本与失败分支。不要将这里的教学模型数字改标为实测。

## 持续验证

GitHub Actions 在干净的 Ubuntu / Node 24 环境执行类型检查、逻辑测试、CPU 实验、静态构建、资源校验，再发布 Pages。发布后的深链接、图片、搜索与刷新另行做在线检查。
