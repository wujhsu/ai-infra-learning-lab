# 验证记录

日期：2026-09-13。验证内容与学习效果分开；全库仍为“待真实试读”。

## 内容与静态构建

- 38 章主线、5 个项目、6 个源码阅读任务、7 个进阶专题。
- 214 页讲义索引、12 场会议参考；正文不依赖会议材料成立。
- 17 个 Mermaid SVG、四组共享数据的逐步数值图、GPU 数据搬运中文图。
- 旧 24 节、7 专题、13 实验文章及旧路由、旧作者生成脚本已删除；不创建归档、跳转或进度迁移。
- Astro 类型检查、生产构建、内部链接/锚点/资源、概念前置关系与私有资料泄漏扫描。
- 正文估时按内容规模估计；实验用时单独依环境，不使用统一 25 分钟。

## 已运行的机制与工程检查

- TypeScript 12 项：六种演示逻辑及边界、共享 Attention/KV 数据、梯度更新、V2 记录与冲突导入。
- Python 11 项：流式解析与失败、发布决策与业务失败、小模型因果/缓存/梯度/恢复/训练。
- CPU 100 步小模型训练：损失约 2.59856 → 0.000875，缓存开关生成一致；只有单句过拟合意义。
- CPU/Gloo 双进程 DDP：与全局批次单进程一次更新最大参数误差 1.49e-8。
- CPU/Gloo DCP 保存加载：恢复后的下一步更新最大参数误差 0；固定数据、无 dropout、相同 world size。
- 原始结果见 runs/*.json。所有 GPU/NCCL/FSDP/LoRA/集群性能与故障实验未运行。

## 实际浏览器检查

使用同一 ego-browser 验证空间，独立本地预览：

- 术语上下文、Enter 打开、Escape 关闭、焦点回到触发词；手机底部卡片与桌面边界位置。
- 阅读字号 20px、行距 2.1 生效；书签、笔记与术语待复习持久保存。
- JSON 与 Markdown 文件真实下载；导入预览显示重复数量，导入前备份、导入成功与笔记保留。
- 中英文搜索 ResourceClaim/梯度返回结果与段落定位；术语别名有单独入门入口。
- 390、768、1440 宽度 × 八个代表页面，共 24 次 DOM 布局检查，无页面横向溢出或失效图片。
- 禁用 JavaScript 后 Attention 正文和数值图存在，术语链接可进入字典。
- DRA 无候选设备分支、图解分步高亮、高清 slide 弹窗正确。

早期验证曾因 Mac 锁屏而无法截图；本轮解锁后已补充桌面、手机、深色主题和章节选择器的像素级视觉审阅。真实读者是否能连续理解仍需反馈。

## 阅读导航与默认展示

- 38 个主线章节全部输出语义化面包屑、56px 章节切换栏、默认展开的编号路标、无 JavaScript 课程入口及正确的相邻章节链接。
- 主线按 10 个阶段显示完整章节选择器；当前阶段默认展开，当前章节高亮并滚入视野。项目、源码阅读和进阶专题使用各自分类与顺序。
- 本章路标、承接前文、关键公式、核心推导和演示默认场景直接可见；练习答案、补充材料、来源证据和高级参数保持折叠。
- 键盘和触屏验证覆盖选择器打开、Escape/外部关闭、焦点恢复、当前小节同步、路标高亮及已读标记。
- 390、768、1440 与横屏 844 宽度共 20 个代表性页面组合无横向溢出、失效图片或小于 44px 的主要操作目标；390 宽度、22px 正文和深色主题组合也通过。
- 禁用 JavaScript 后，正文、默认展开路标、承接前文、演示默认数值、面包屑、相邻章节与完整课程入口仍可用。

## 发布

2026-09-13 已将 `d3b4449` 使用 GitHub Actions 干净构建并部署至现有 GitHub Pages。[构建与部署记录](https://github.com/wujhsu/ai-infra-learning-lab/actions/runs/34753120069)。Linux CI 也实际执行通过 TypeScript/Python 测试、双进程 CPU DDP 与 DCP 恢复检查。

部署后实际验证：

- 首页、生成/Attention/DRA 章节、极小模型项目、vLLM 源码阅读均返回 HTTP 200。
- Pagefind 脚本、GPU 图与实验 ZIP 下载返回 HTTP 200。
- 旧 `/learn/01-request/`、`/labs/01-serving/`、`/topics/01-kernelagent/`、`/foundations/` 返回 HTTP 404；构建检查同时确认全部旧路由均未生成。
- 线上浏览器中 ResourceClaim 与“梯度”均返回新内容及段落链接；ResourceClaim 首项为新 DRA 章节，概念入口直接指向首次讲解。
- 直接访问并刷新 Attention 章节仍可阅读；无横向溢出或失效的已加载图片资源。
- 线上手机宽度下术语卡片贴底、内容正确，Escape 关闭后焦点恢复。

原始 HTTP 检查结果见 `runs/online-http.json`。像素级视觉审阅、真实试读以及 GPU/集群实验仍保留上述限制。
