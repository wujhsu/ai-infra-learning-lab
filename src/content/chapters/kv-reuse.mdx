---
title: "为什么历史计算可以复用？"
description: "追踪连续两轮生成，弄清 K/V 从哪里来、什么时候能复用，以及缓存没有省掉的工作。"
order: 4
stage: 1
minutes: 16
revision: 2
status: "preview"
terms: ["kv-cache", "prefill", "decode"]
sources: ["hf-cache", "attention", "pagedattention"]
sessions: []
prerequisites: ["attention", "generation"]
question: "输入 4 个位置，已经输出 2 个新 token，但只把第一个新 token 送回模型处理。此时完整历史缓存包含几个位置？"
answer: "包含 5 个已处理位置：4 个输入加上被送回模型的第一个输出。第二个输出目前只有 ID，尚未产生其各层 K/V。这个回答针对本章的普通逐 token 循环与完整历史缓存。"
bridge: "下一步：模型怎样知道参数该往哪边改？把本章的数据与状态继续放进下一段执行过程。"
verification: "机制与代码审校；待真实试读"
project: "tiny-model"
introduces: ["prefill", "decode", "kv-cache"]
requires: []
---
import Term from '../../components/Term.astro';
import LearningFigure from '../../components/LearningFigure.astro';
import Demo from '../../components/Demo';
import Slide from '../../components/Slide.astro';
import Diagram from '../../components/Diagram.astro';
import {asset} from '../../data/curriculum';

上一章，我们让每个位置根据自己的 Q 读取允许访问的 K/V。现在回到第一章的循环：已经根据“北京是中国的”选出“首都”，下一步还想生成句号。

最直接的方法是重新输入整段“北京是中国的首都”，从头计算。程序容易理解，但里面是否做了重复工作？我们从数据依赖回答，而不是先背一种优化的名字。

<span id="two-rounds"></span>
## 两轮输入中，哪些位置真的发生了变化？

第一轮有四个位置，第二轮有五个。前四个 token、位置和模型参数都没变。新增的只有位置 4 的“首都”。

由于因果遮罩，位置 0 不读取位置 1 之后的内容；位置 1 不读取位置 2 之后的内容。以此类推，前四个位置都不会读取新增的“首都”。所以后面的内容增加，并不会改变这些位置在同一层应算出的表示。

这一点可以逐层推下去：第一层里旧位置结果不变，下一层收到的旧位置输入也不变；固定的后续运算仍产生同样的旧位置结果。这需要模型使用适当的因果结构、相同参数和一致的位置处理，并在推理模式下排除随机训练行为。

**旧位置的表示不变，因此从这些表示生成的 K/V 也不变。** 保存它们，下次就可以直接读取。这就是 <Term id="kv-cache"/>。

注意论证的顺序：不是“因为有缓存，所以旧结果不变”；是先有依赖关系保证不变，缓存复用才正确。

## 为什么保存 K/V，而不是把所有东西都留下？

为新增位置计算 Attention 时，需要当前 Q 与所有可见 K 的匹配结果，再汇总所有可见 V。历史位置的 Q 并不在这一步需要的输入里。

因此，通常保存各层已经处理过的 K/V。当前新位置的 Q 用完后，不必仅为了下一个新位置继续保留。你仍需要计算新的 Q，并且每一层都有自己的缓存，而不是把第一层的 K/V 直接交给所有层使用。

| 数据 | 产生方式 | 后续生成时的用途 |
|---|---|---|
| 模型权重 | 训练得到 | 各轮持续读取，一般不因请求变长而增加 |
| 输入与输出 token ID | 分词或本轮选择 | 记录上下文和生成结果 |
| 旧位置 K/V | 旧位置经过本层计算产生 | 被新位置的 Q 读取，不重复生成 |
| 新位置 Q/K/V | 新位置经过本层计算产生 | Q 用于本轮匹配；K/V 追加到缓存 |
| 最终回答文本 | 输出 ID 解码得到 | 发给用户，不是 Attention 所需缓存 |

这也说明 KV Cache 与常见的 HTTP 响应缓存不同。前者保留计算中间量，让模型继续生成；后者可能直接返回已有答案，不再执行生成。

<LearningFigure kind="cache" />

## “选出了首都”和“缓存包含首都”不是同一时刻

这是实现生成循环时很容易错一位的地方。处理完四个输入位置，模型的最后位置输出用于选择“首都”。刚选出时，我们只有它的 ID，还没让这个新位置经过模型。

下一轮把“首都”作为新输入送入模型，才会计算它在各层的 K/V。随后利用包含五个位置的缓存，决定再后面的 token。

```text
处理 4 个输入位置 → KV 长度 4 → 选出“首都”
处理“首都”        → KV 长度 5 → 选出“。”
处理“。”          → KV 长度 6 → 选出结束标记
```

如果结束后不再调用模型，最后选出的结束标记也就无需进入缓存。由此可见，不能在每个时刻都用“输入 token 数 + 已输出 token 数”机械推断当前已物化的缓存长度；还要看最后输出是否已被再次处理。

## 比较有缓存与无缓存的计算

先考虑单层、单头、二维表示的教学模型。旧四个位置的 K/V 已经存在。新增“首都”的向量是 `[2,0]`，使用同一套权重得到 Q=`[2,0]`、K=`[2,0]`、V=`[4,0]`。

现在把新 K/V 追加到旧四组后面，用新 Q 对这五个位置算 Attention。所得新位置输出，应与把五个位置全部重新算一遍后取最后一行一致。

```python
# 依赖关系伪代码；完整实现见极小模型项目。
q_new, k_new, v_new = project(new_position)
K = concat(old_K, k_new)
V = concat(old_V, v_new)
new_output = attention(q_new, K, V)
```

这里 `concat` 表示沿位置维度连接。真实引擎可以使用预分配或分页内存，避免每一步复制整个数组；这是存储实现的优化，不改变“本轮能读到旧位置和新增位置”的逻辑。

验证缓存正确性时，应该比较数值结果，不能只看两种方式都生成了看起来合理的中文。项目会比较全量因果计算与逐位置缓存计算的每个位置输出，并给浮点计算设置误差容限。

<span id="phases"></span>
## Prefill 与 Decode 是怎样自然出现的？

第一次拿到整段已知输入，可以一起处理这些位置并建立缓存。这个阶段叫 <Term id="prefill"/>，中文常叫预填充。使用最后位置的输出，能选择第一个新 token。

此后每一轮在已有缓存基础上处理新位置，继续生成，这个执行阶段叫 <Term id="decode"/>。它与“把 ID 解码成文本”使用了同一个英文词，但在推理引擎讨论里指不同事情。

不要把 Prefill 简化为“只是写缓存”。它仍在执行模型计算；也不要把 Decode 理解为“只是查缓存”。新位置仍要经过各层，产生新表示与输出得分。

提示词越长，Prefill 需要处理的已知位置越多。Decode 每轮新增位置少，却要读取不断积累的历史 K/V。这种工作结构差异，会影响 GPU 的计算与数据搬运比例；我们将在硬件与推理性能章节用测量验证。

## 缓存省了什么，没有省什么？

在普通全历史因果 Attention 中，复用缓存省掉了旧位置的重复投影和相应的重复前向工作。但是新 Q 仍然要与可见历史 K 匹配，并使用历史 V。

因此，在这个普通注意力模型里，单个新位置的注意力工作量仍随可见历史长度增加。缓存让“重新产生旧数据”减少了，不等于让“读取旧数据”消失了。

缓存还占空间。单层单头、每个 K 和 V 都有 d 个分量时，n 个位置共存 `2 × n × d` 个数。这里的 2 来自 K 和 V 两份，不是两个请求。

本例 n=4、d=2，共 16 个数。若每个数 4 字节，原始 K/V 数据占 64 字节；新增一个位置，增加 4 个数，即 16 字节。真实模型还要乘层数、KV 头数、并发等因素，后面的容量章节会逐项展开，避免突然给一个巨大公式。

## 哪些变化会破坏复用条件？

如果把前缀里的“中国”改成别的词，后面位置的输入上下文变了。不能把旧请求中相同位置编号的 K/V 无条件搬过来。如果模型权重或适配器改变，或者位置处理、输入模态等影响计算的条件不同，也必须重新判断兼容性。

多个请求复用相同前缀时，需要匹配影响计算的完整条件，不只是字符串“看起来相似”。跨请求缓存还涉及租户隔离和生命周期；等学习缓存策略时再把这些系统约束加上。

滑动窗口等模型只读取部分历史，缓存策略可能淘汰更早位置；混合或非 Transformer 结构也可能使用不同状态。这里得到的是普通因果 Attention 的基础机制，不是所有架构都必须照用的内存布局。

## 先理解，再做两个反例

第一个反例：模型参数相同，但偷偷更换前缀一个 token，仍拿旧 K/V 计算。结果未必正确，因为保存的是与原上下文相关的数值。

第二个反例：缓存正确，但把长上下文下的慢响应全部归因于“没有命中”。即使命中，历史数据读取也有代价，队列和其他工作还可能占时间。

这两类错误分别是正确性问题和性能归因问题。AI Infra 的优化通常需要同时检查二者。
