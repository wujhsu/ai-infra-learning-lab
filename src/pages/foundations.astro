---
import Base from '../layouts/Base.astro';
import {asset} from '../data/curriculum';
---
<Base title="数学与软件基础补课"><div class="page-wrap" style="max-width:850px"><header class="page-title"><span class="eyebrow">随用随查 · 约 20 分钟</span><h1>读懂教材需要的几块积木</h1><p>无需先学完整门数学课。遇到障碍时，只补当前需要的一块，再回到原问题。</p></header><article class="prose" data-pagefind-body>
<h2 id="shape">先读形状，再看数字</h2>
<p>标量是一个数，例如温度 25；向量是一列数，例如一个 token 的 128 个特征；矩阵是二维表；张量（Tensor）把这种组织扩展到更多维。<code>[B, T, H, D]</code> 可以表示批次、序列位置、注意力头、每头特征，但字母只是约定，必须看当前代码定义。</p>
<p>形状为 [2, 3] 的数组有 6 个元素；若每元素占 2 字节，原始数据占 12 字节。乘维度得到的是元素数，再乘 dtype 字节数才得到存储量。实际程序还可能有内存对齐、对象和分配器开销。</p>
<h2 id="matmul">矩阵乘法：中间维度必须相同</h2>
<p>A 的形状为 [m, k]，B 为 [k, n]，结果 C 为 [m, n]。每个 C[i,j] 是 A 的第 i 行与 B 的第 j 列逐项相乘后相加。如果 A 的一行是 [1,2]，B 的一列是 [3,4]，对应输出就是 1×3+2×4=11。</p>
<p>由此可以估算普通稠密矩阵乘的工作量：大约 m×n×k 次乘法和相近数量加法，合称约 2mnk FLOPs。FLOPs 是运算次数，FLOP/s 才是速度。向量点积只是矩阵乘法的一个小尺寸情形。</p>
<h2 id="transpose">转置、广播和内存连续</h2>
<p>转置把行列角色交换，[m,n] 变为 [n,m]。有些张量库只改变读取的步长（stride），不立刻搬动底层数据，所以“形状对了”不代表内存连续。广播（broadcasting）按规则扩展大小为 1 的维度，用于同一偏置加到多行；并非任意不相同的形状都能相加。</p>
<h2 id="softmax">Softmax 为什么不是简单取最大</h2>
<p>注意力先得到每个历史位置的分数，再转为一组非负、总和为 1 的权重。Softmax 将每个分数取指数后除以指数之和。计算时常先减去最大分数，避免大指数溢出，这不会改变最终比例。</p>
<p>两个分数相同，会得到各 1/2；一个更大，它的权重通常更大，但其他位置不一定变成 0。因果 mask 将未来位置排除，确保生成当前位置时不能偷看未来。<a href={asset('labs/02-attention/')}>在无 GPU 的小实验中手算并验证 →</a></p>
<h2 id="units">单位先对齐，公式才有意义</h2>
<p>1 字节（byte）=8 位（bit）；1 GB=10⁹ 字节，1 GiB=2³⁰ 字节。网速常用 Gbit/s，显存带宽常用 GB/s，差一个 8 倍不能忽略。教材容量计算器用 GiB，分层缓存加载模型用十进制 GB 和 GB/s，并分别标注。</p>
<p>搬运 4 GB 数据，若有效带宽 8 GB/s，理想搬运时间为 4÷8=0.5 秒。这里的“有效”包含实际路径损耗；接口标称带宽不能直接当成应用带宽。时间公式中 1 秒=1000 毫秒（ms）=10⁶ 微秒（μs）。</p>
<h2 id="percentile">平均数、分位数与实验变量</h2>
<p>10 个延迟样本排序后，nearest-rank p90 取第 ceil(0.9×10)=9 个值。平均值描述总量平均分配，分位数描述有多少请求落在某个界限以内。样本只有 10 个时，不足以稳定估计 p99。</p>
<p>每次只改变一个主要变量，把其他条件记下来：模型 revision、输入输出长度、并发、缓存冷暖、硬件与版本。先预测变化方向，再测量，再解释偏差；这比搜到一个“最佳参数”直接套用更可靠。</p>
<h2 id="process">进程、线程、容器和虚拟机</h2>
<p>进程是操作系统运行程序的实例；线程在进程内部共享地址空间并执行工作。容器利用操作系统隔离和资源控制包装进程，通常共享宿主内核。虚拟机运行自己的客户机内核。容器镜像是可复用的文件系统与启动配置，运行中的容器才是实例。</p>
<p>因此，镜像里有 CUDA 用户态库，不代表宿主 GPU 驱动已经正确安装；在自己电脑能运行，也不代表集群节点能够访问同一模型文件。一个 Pod 可以有多个共享网络等资源的容器，Pod 的 IP 不一定长期保持。</p>
<h2 id="network">地址、端口、DNS 和健康检查</h2>
<p>IP 地址定位网络端点，端口区分同一地址上的不同服务。DNS 把名称解析成地址；DNS 成功只能说明解析成功，不能证明目标端口开放或应用健康。<code>127.0.0.1</code> 是当前网络空间中的自己，容器内的 localhost 通常不等于宿主机器。</p>
<p>检查请求链可从名称解析、网络连接、HTTP 状态、返回内容逐层推进。<code>/health</code> 返回 200 也不代表真实推理请求满足延迟和正确性要求，这正是发布实验中健康探针与业务分析分开的原因。</p>
<h2 id="async">异步执行为什么要同步后计时</h2>
<p>CPU 发出 GPU 工作后可能立即返回，此时 GPU 还没有算完。只测函数调用前后，可能主要测到了排队或发起工作的开销。基准需要预热，并在约定位置同步或使用设备事件；生产代码却不应为了计时随意插入大量同步，因为那会改变被测系统。</p>
<h2 id="read-code">读一段陌生代码的顺序</h2>
<p>先找输入和输出形状，再找数据从哪里来、写到哪里；接着看谁负责启动、谁在等待谁，最后检查错误与清理路径。读分布式代码时额外标出 rank、设备与通信顺序。看懂一个小输入的完整执行过程后，再考虑性能优化。</p>
<p>这页是教学基础推导。具体库的行为请结合课程对应的 <a href={asset('sources/')}>官方来源与版本</a>，不要把示意模型当成所有实现的规则。</p>
<a class="button" href={asset('path/')}>回到学习路径 →</a>
</article></div></Base>
