---
import Base from '../layouts/Base.astro';import LearningFigure from '../components/LearningFigure.astro';import {asset,reading,projects} from '../data/curriculum';import type {Project} from '../data/curriculum';
const span=(p:Project)=>{const first=reading.find(x=>x.id===p.chapters[0])!.order,last=reading.find(x=>x.id===p.chapters[p.chapters.length-1])!.order;return first===last?`第 ${first} 章`:`第 ${first}–${last} 章`;};
---
<Base title="从模型计算到训练、推理与平台工程"><div class="home-reader"><div class="home-hero"><div class="home-hero-copy"><span class="eyebrow">面向会编程、刚进入 AI Infra 的你</span><h1>把模型内部的计算，<br/>接到真实运行的系统。</h1><p class="home-intro">从一个 token、一行向量开始。亲手实现模型，理解训练与推理，再一步步走到 GPU、分布式和平台工程。</p><section class="start-reading"><div><small data-continue-label>第一章 · 从这里开始</small><h2 data-continue-title>模型怎样接着写下去？</h2><p data-continue-question>一次计算为什么只够决定下一个 token？沿着同一段中文，看清生成的循环。</p></div><a class="button" data-continue href={asset('read/generation/')}>开始阅读 →</a></section></div><div class="home-hero-figure"><LearningFigure kind="generation" /></div></div><p class="trial-note">新版内容已按项目主线组织；学习体验待真实试读。实验是否运行，见各项目的验证记录。</p><div class="home-projects">{projects.map(p=><a class="home-project-card" href={asset(`path/#${p.id}`)}><small>{p.label} · {span(p)}</small><h2>{p.title}</h2><p>{p.outcome}</p><span class="home-project-lab">{p.labStatus}</span></a>)}</div><div class="home-next"><a href={asset('path/')}><strong>完整课程主线 →</strong><p>五个项目串起全部 38 章，随时回看前置概念。</p></a><a href={asset('projects/')}><strong>把知识做成系统 →</strong><p>进入每个项目的动手实践与验证记录。</p></a></div><details class="library-more"><summary>查概念、读源码或寻找参考资料</summary><div class="feature-strip"><a href={asset('glossary/')}>概念索引</a><a href={asset('code/')}>源码阅读</a><a href={asset('advanced/')}>进阶研究</a><a href={asset('sources/')}>官方资料</a><a href={asset('sessions/')}>会议参考</a></div></details><div hidden>{reading.map(c=><span data-course={`read/${c.id}`} data-title={c.title} data-question={`接着理解：${c.title} 从上次记录的位置继续。`}/>)}</div></div></Base>
